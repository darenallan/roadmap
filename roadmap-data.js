window.ROADMAP_DATA=[{"code": "A", "name": "Sécurité critique & bugs bloquants", "intro": "> Correctifs indépendants les uns des autres, à traiter en premier car ils touchent des flux déjà en production (tunnel de conversion vendeur, upload produit).", "steps": [{"id": 1, "title": "Sécuriser le mot de passe admin exposé en clair dans git", "objective": "Éliminer le secret en clair committé dans le dépôt et empêcher sa réapparition.", "files": "`server-sanhia/scripts/create-users.js`, `server-sanhia/.env`, `server-sanhia/.gitignore`", "instructions": "Déplacer l'email/mot de passe admin (et tout compte de test) de `create-users.js` vers des variables d'environnement (`ADMIN_EMAIL`, `ADMIN_PASSWORD`, etc.) lues via `process.env`. Le script doit échouer explicitement si ces variables sont absentes plutôt que d'utiliser une valeur par défaut. Ajouter ces clés à `.env` (jamais committé) et à un `.env.example` mis à jour avec des placeholders. Noter dans un commentaire du script qu'une rotation du mot de passe Firebase réel est nécessaire côté humain (hors périmètre de cette étape — action manuelle sur le compte Firebase).", "validation": "`grep -r \"password.*:.*['\\\"]\" scripts/create-users.js` ne renvoie plus aucune valeur en clair ; le script lève une erreur claire si `ADMIN_PASSWORD` n'est pas défini ; `.env.example` contient les nouvelles clés sans valeur réelle.", "originalStatus": "[x] Terminé (2026-08-22)", "status": "todo", "completedAt": "2026-08-22", "assignee": "", "note": "", "raw": "- **Objectif :** Éliminer le secret en clair committé dans le dépôt et empêcher sa réapparition.\n- **Fichiers impactés :** `server-sanhia/scripts/create-users.js`, `server-sanhia/.env`, `server-sanhia/.gitignore`\n- **Instructions d'exécution :** Déplacer l'email/mot de passe admin (et tout compte de test) de `create-users.js` vers des variables d'environnement (`ADMIN_EMAIL`, `ADMIN_PASSWORD`, etc.) lues via `process.env`. Le script doit échouer explicitement si ces variables sont absentes plutôt que d'utiliser une valeur par défaut. Ajouter ces clés à `.env` (jamais committé) et à un `.env.example` mis à jour avec des placeholders. Noter dans un commentaire du script qu'une rotation du mot de passe Firebase réel est nécessaire côté humain (hors périmètre de cette étape — action manuelle sur le compte Firebase).\n- **Critères de validation :** `grep -r \"password.*:.*['\\\"]\" scripts/create-users.js` ne renvoie plus aucune valeur en clair ; le script lève une erreur claire si `ADMIN_PASSWORD` n'est pas défini ; `.env.example` contient les nouvelles clés sans valeur réelle.\n- **Statut :** [x] Terminé (2026-08-22)"}, {"id": 2, "title": "Corriger le lien mort du tunnel de conversion vendeur (web)", "objective": "Réparer les CTA de la page marketing vendeur qui pointent vers une route inexistante.", "files": "`sanhia-web/src/pages/SellerOnboarding.jsx`", "instructions": "Remplacer les deux occurrences de `<Link to=\"/seller-application\">` par `<Link to=\"/seller/application\">`, conformément à la route déclarée dans `sanhia-web/src/App.jsx`.", "validation": "`grep -n \"seller-application\" sanhia-web/src/pages/SellerOnboarding.jsx` ne renvoie plus aucun résultat ; cliquer sur \"Déposer ma candidature\" depuis `/seller/onboarding` ouvre bien le formulaire de candidature (pas de 404).", "originalStatus": "[x] Terminé (2026-08-23)", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Réparer les CTA de la page marketing vendeur qui pointent vers une route inexistante.\n- **Fichiers impactés :** `sanhia-web/src/pages/SellerOnboarding.jsx`\n- **Instructions d'exécution :** Remplacer les deux occurrences de `<Link to=\"/seller-application\">` par `<Link to=\"/seller/application\">`, conformément à la route déclarée dans `sanhia-web/src/App.jsx`.\n- **Critères de validation :** `grep -n \"seller-application\" sanhia-web/src/pages/SellerOnboarding.jsx` ne renvoie plus aucun résultat ; cliquer sur \"Déposer ma candidature\" depuis `/seller/onboarding` ouvre bien le formulaire de candidature (pas de 404).\n- **Statut :** [x] Terminé (2026-08-23)"}, {"id": 3, "title": "Corriger le Content-Type multipart sur l'ajout de produit (web)", "objective": "Réparer l'upload d'images produit, cassé par un `Content-Type` fixé manuellement sans boundary.", "files": "`sanhia-web/src/pages/Seller.jsx` (composant `AddProductTab`)", "instructions": "Retirer la ligne `'Content-Type': 'multipart/form-data'` de l'objet `headers` passé à l'appel `axios.post` d'ajout de produit — ne garder que `Authorization`. Le navigateur générera lui-même le `Content-Type` avec le bon `boundary` à partir du `FormData`. Ajouter un commentaire expliquant pourquoi (même bug déjà corrigé dans `SellerApplication.jsx`).", "validation": "Publier un produit avec au moins une image depuis `/seller` aboutit à un produit visible dans \"Mes Produits\" avec son image, sans erreur 400/500 en console réseau.", "originalStatus": "[x] Terminé (2026-08-23)", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Réparer l'upload d'images produit, cassé par un `Content-Type` fixé manuellement sans boundary.\n- **Fichiers impactés :** `sanhia-web/src/pages/Seller.jsx` (composant `AddProductTab`)\n- **Instructions d'exécution :** Retirer la ligne `'Content-Type': 'multipart/form-data'` de l'objet `headers` passé à l'appel `axios.post` d'ajout de produit — ne garder que `Authorization`. Le navigateur générera lui-même le `Content-Type` avec le bon `boundary` à partir du `FormData`. Ajouter un commentaire expliquant pourquoi (même bug déjà corrigé dans `SellerApplication.jsx`).\n- **Critères de validation :** Publier un produit avec au moins une image depuis `/seller` aboutit à un produit visible dans \"Mes Produits\" avec son image, sans erreur 400/500 en console réseau.\n- **Statut :** [x] Terminé (2026-08-23)"}, {"id": 4, "title": "Corriger le Content-Type multipart sur l'upload d'avatar (web)", "objective": "Réparer l'upload de photo de profil, potentiellement cassé par le même bug.", "files": "`sanhia-web/src/pages/Profile.jsx`", "instructions": "Même correctif qu'à l'Étape 3 : retirer le `Content-Type` manuel de l'appel d'upload d'avatar, ne garder que `Authorization`.", "validation": "Changer sa photo de profil depuis `/profile` aboutit à un avatar mis à jour et persistant après rechargement de la page.", "originalStatus": "[x] Terminé (2026-08-23)", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Réparer l'upload de photo de profil, potentiellement cassé par le même bug.\n- **Fichiers impactés :** `sanhia-web/src/pages/Profile.jsx`\n- **Instructions d'exécution :** Même correctif qu'à l'Étape 3 : retirer le `Content-Type` manuel de l'appel d'upload d'avatar, ne garder que `Authorization`.\n- **Critères de validation :** Changer sa photo de profil depuis `/profile` aboutit à un avatar mis à jour et persistant après rechargement de la page.\n- **Statut :** [x] Terminé (2026-08-23)"}, {"id": 5, "title": "Réparer la sanitisation XSS contournée sur les routes multipart (backend)", "objective": "Garantir que les champs texte soumis en `multipart/form-data` passent par la sanitisation anti-XSS, comme le JSON.", "files": "`server-sanhia/routes/products.js`, `server-sanhia/routes/users.routes.js`, `server-sanhia/routes/seller-apply.routes.js`, `server-sanhia/middleware/sanitize.js`", "instructions": "Exporter depuis `middleware/sanitize.js` une fonction `sanitizeObject(obj)` réutilisable (extraire la logique actuellement inline dans le middleware global). Dans chaque route utilisant `multer` (création produit, upload avatar, candidature vendeur), appeler `req.body = sanitizeObject(req.body)` juste après le middleware `upload`/`multer`, avant toute lecture des champs.", "validation": "Soumettre un produit avec `name: \"<script>alert(1)</script>Test\"` via le formulaire d'ajout produit ; le nom stocké en base ne contient plus la balise `<script>` brute.", "originalStatus": "[x] Terminé (2026-08-23)", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Garantir que les champs texte soumis en `multipart/form-data` passent par la sanitisation anti-XSS, comme le JSON.\n- **Fichiers impactés :** `server-sanhia/routes/products.js`, `server-sanhia/routes/users.routes.js`, `server-sanhia/routes/seller-apply.routes.js`, `server-sanhia/middleware/sanitize.js`\n- **Instructions d'exécution :** Exporter depuis `middleware/sanitize.js` une fonction `sanitizeObject(obj)` réutilisable (extraire la logique actuellement inline dans le middleware global). Dans chaque route utilisant `multer` (création produit, upload avatar, candidature vendeur), appeler `req.body = sanitizeObject(req.body)` juste après le middleware `upload`/`multer`, avant toute lecture des champs.\n- **Critères de validation :** Soumettre un produit avec `name: \"<script>alert(1)</script>Test\"` via le formulaire d'ajout produit ; le nom stocké en base ne contient plus la balise `<script>` brute.\n- **Statut :** [x] Terminé (2026-08-23)"}, {"id": 6, "title": "Ajouter un garde-fou contre la bascule silencieuse en mode mock (web)", "objective": "Empêcher que l'app parte en mode simulation (OTP factice, données mock) sans avertissement si la config Firebase est absente en production.", "files": "`sanhia-web/src/context/AuthContext.jsx`, `sanhia-web/src/main.jsx`", "instructions": "Dans `AuthContext.jsx`, si `isMockMode` est vrai ET `import.meta.env.PROD` est vrai, logger une erreur explicite en console (`console.error('⚠️ Mode mock actif en production — vérifier VITE_FIREBASE_API_KEY')`) au chargement du module. Ne pas bloquer l'app (éviter un écran blanc en cas de vraie mauvaise config), mais rendre l'anomalie impossible à manquer en observabilité.", "validation": "Builder l'app avec `VITE_FIREBASE_API_KEY` volontairement absent et `NODE_ENV=production` fait apparaître le message d'erreur en console au premier chargement.", "originalStatus": "[x] Terminé (2026-08-23)\n\n---", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Empêcher que l'app parte en mode simulation (OTP factice, données mock) sans avertissement si la config Firebase est absente en production.\n- **Fichiers impactés :** `sanhia-web/src/context/AuthContext.jsx`, `sanhia-web/src/main.jsx`\n- **Instructions d'exécution :** Dans `AuthContext.jsx`, si `isMockMode` est vrai ET `import.meta.env.PROD` est vrai, logger une erreur explicite en console (`console.error('⚠️ Mode mock actif en production — vérifier VITE_FIREBASE_API_KEY')`) au chargement du module. Ne pas bloquer l'app (éviter un écran blanc en cas de vraie mauvaise config), mais rendre l'anomalie impossible à manquer en observabilité.\n- **Critères de validation :** Builder l'app avec `VITE_FIREBASE_API_KEY` volontairement absent et `NODE_ENV=production` fait apparaître le message d'erreur en console au premier chargement.\n- **Statut :** [x] Terminé (2026-08-23)\n\n---"}]}, {"code": "B", "name": "Nettoyage et dette qui bloque la suite", "intro": "> Code mort et incohérences à retirer avant d'ajouter de nouvelles routes dans les mêmes fichiers, pour ne pas complexifier davantage un état déjà confus.", "steps": [{"id": 7, "title": "Supprimer le middleware d'authentification mort et unifier la vérification admin (backend)", "objective": "Éliminer la duplication de logique d'auth (3 implémentations concurrentes) avant d'ajouter de nouvelles routes admin.", "files": "`server-sanhia/middleware/auth.js` (supprimé), `server-sanhia/routes/admin.routes.js`", "instructions": "Supprimer le fichier `middleware/auth.js` (confirmé non importé nulle part). Dans `admin.routes.js`, remplacer la vérification `isAdmin` inline par la composition du middleware `verifyToken` existant (`middleware/verifyToken.js`) suivi d'une vérification `req.user.role === 'ADMIN'` factorisée dans une petite fonction `requireAdmin` locale au fichier (ou dans `middleware/verifyToken.js` sous forme d'export additionnel).", "validation": "`grep -r \"middleware/auth\" server-sanhia/routes` ne renvoie aucun résultat ; toutes les routes de `admin.routes.js` restent inaccessibles (403) à un compte non-admin après le changement.", "originalStatus": "[x] Terminé (2026-08-23)", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Éliminer la duplication de logique d'auth (3 implémentations concurrentes) avant d'ajouter de nouvelles routes admin.\n- **Fichiers impactés :** `server-sanhia/middleware/auth.js` (supprimé), `server-sanhia/routes/admin.routes.js`\n- **Instructions d'exécution :** Supprimer le fichier `middleware/auth.js` (confirmé non importé nulle part). Dans `admin.routes.js`, remplacer la vérification `isAdmin` inline par la composition du middleware `verifyToken` existant (`middleware/verifyToken.js`) suivi d'une vérification `req.user.role === 'ADMIN'` factorisée dans une petite fonction `requireAdmin` locale au fichier (ou dans `middleware/verifyToken.js` sous forme d'export additionnel).\n- **Critères de validation :** `grep -r \"middleware/auth\" server-sanhia/routes` ne renvoie aucun résultat ; toutes les routes de `admin.routes.js` restent inaccessibles (403) à un compte non-admin après le changement.\n- **Statut :** [x] Terminé (2026-08-23)"}, {"id": 8, "title": "Supprimer le dossier `controllers/` vide (backend)", "objective": "Retirer un vestige d'architecture antérieure qui n'a plus d'usage.", "files": "`server-sanhia/controllers/` (supprimé)", "instructions": "Vérifier qu'aucun fichier de `routes/` n'importe quoi que ce soit depuis `controllers/` (`grep -r \"require.*controllers\" server-sanhia/routes`), puis supprimer le dossier.", "validation": "Le dossier `server-sanhia/controllers/` n'existe plus ; `npm start` (ou `npm run dev`) démarre sans erreur de module manquant.", "originalStatus": "[x] Terminé (2026-08-23) — dossier déjà absent (probablement perdu lors du re-clone, git ne trace pas les dossiers vides), rien à faire", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Retirer un vestige d'architecture antérieure qui n'a plus d'usage.\n- **Fichiers impactés :** `server-sanhia/controllers/` (supprimé)\n- **Instructions d'exécution :** Vérifier qu'aucun fichier de `routes/` n'importe quoi que ce soit depuis `controllers/` (`grep -r \"require.*controllers\" server-sanhia/routes`), puis supprimer le dossier.\n- **Critères de validation :** Le dossier `server-sanhia/controllers/` n'existe plus ; `npm start` (ou `npm run dev`) démarre sans erreur de module manquant.\n- **Statut :** [x] Terminé (2026-08-23) — dossier déjà absent (probablement perdu lors du re-clone, git ne trace pas les dossiers vides), rien à faire"}, {"id": 9, "title": "Supprimer le composant `Placeholder.js` orphelin (mobile)", "objective": "Retirer un composant devenu mort après le remplacement de tous les écrans placeholder par de vrais écrans.", "files": "`sanhia-mobile/src/components/Placeholder.js` (supprimé), `sanhia-mobile/README.md`", "instructions": "Confirmer qu'aucun écran sous `app/` n'importe `Placeholder` (`grep -rn \"Placeholder\" sanhia-mobile/app`), supprimer le fichier, et retirer sa mention de `README.md`.", "validation": "`npx expo export --platform web` (depuis `sanhia-mobile/`) réussit sans erreur d'import manquant.", "originalStatus": "[x] Terminé (2026-08-23)", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Retirer un composant devenu mort après le remplacement de tous les écrans placeholder par de vrais écrans.\n- **Fichiers impactés :** `sanhia-mobile/src/components/Placeholder.js` (supprimé), `sanhia-mobile/README.md`\n- **Instructions d'exécution :** Confirmer qu'aucun écran sous `app/` n'importe `Placeholder` (`grep -rn \"Placeholder\" sanhia-mobile/app`), supprimer le fichier, et retirer sa mention de `README.md`.\n- **Critères de validation :** `npx expo export --platform web` (depuis `sanhia-mobile/`) réussit sans erreur d'import manquant.\n- **Statut :** [x] Terminé (2026-08-23)"}, {"id": 10, "title": "Configurer `trust proxy` explicitement (backend)", "objective": "Garantir que le rate limiting par IP fonctionne correctement derrière le reverse proxy de Render.", "files": "`server-sanhia/index.js`", "instructions": "Ajouter `app.set('trust proxy', 1)` juste après l'initialisation d'`app`, avant le montage des middlewares `cors`/`rateLimit`.", "validation": "Une requête avec un en-tête `X-Forwarded-For` simulé (test local via `curl -H \"X-Forwarded-For: 1.2.3.4\"`) est bien comptabilisée sous cette IP côté rate limiter, pas sous l'IP du proxy.", "originalStatus": "[x] Terminé (2026-08-23)", "status": "todo", "completedAt": "2026-08-23", "assignee": "", "note": "", "raw": "- **Objectif :** Garantir que le rate limiting par IP fonctionne correctement derrière le reverse proxy de Render.\n- **Fichiers impactés :** `server-sanhia/index.js`\n- **Instructions d'exécution :** Ajouter `app.set('trust proxy', 1)` juste après l'initialisation d'`app`, avant le montage des middlewares `cors`/`rateLimit`.\n- **Critères de validation :** Une requête avec un en-tête `X-Forwarded-For` simulé (test local via `curl -H \"X-Forwarded-For: 1.2.3.4\"`) est bien comptabilisée sous cette IP côté rate limiter, pas sous l'IP du proxy.\n- **Statut :** [x] Terminé (2026-08-23)"}, {"id": 11, "title": "Exempter/assouplir le rate limiting sur le polling de la messagerie (backend)", "objective": "Éviter qu'une conversation ouverte (polling 3-5s recommandé par le code lui-même) épuise le quota global de l'utilisateur sur toute l'API.", "files": "`server-sanhia/index.js`, `server-sanhia/routes/messages.routes.js`, `server-sanhia/middleware/` (nouveau limiter dédié optionnel)", "instructions": "Créer un limiter dédié plus permissif (ex. 120 requêtes/min) pour `GET /api/messages/conversations/:id/messages` et `GET /api/messages/conversations`, appliqué uniquement à ces deux routes. Le rate limiter global de `index.js` reste inchangé pour le reste de l'API.", "validation": "20 requêtes successives vers `GET /api/messages/conversations/:id/messages` en moins de 60 secondes ne renvoient aucun 429 ; une même rafale vers une autre route (ex. `GET /api/products`) reste soumise à la limite globale existante.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Éviter qu'une conversation ouverte (polling 3-5s recommandé par le code lui-même) épuise le quota global de l'utilisateur sur toute l'API.\n- **Fichiers impactés :** `server-sanhia/index.js`, `server-sanhia/routes/messages.routes.js`, `server-sanhia/middleware/` (nouveau limiter dédié optionnel)\n- **Instructions d'exécution :** Créer un limiter dédié plus permissif (ex. 120 requêtes/min) pour `GET /api/messages/conversations/:id/messages` et `GET /api/messages/conversations`, appliqué uniquement à ces deux routes. Le rate limiter global de `index.js` reste inchangé pour le reste de l'API.\n- **Critères de validation :** 20 requêtes successives vers `GET /api/messages/conversations/:id/messages` en moins de 60 secondes ne renvoient aucun 429 ; une même rafale vers une autre route (ex. `GET /api/products`) reste soumise à la limite globale existante.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "C", "name": "Modération boutique", "intro": "> Bug ciblé qui bloque la moitié d'un flux MVP prioritaire. Pas de dépendance de schéma (le champ `status` existe déjà).", "steps": [{"id": 12, "title": "Ajouter la route d'approbation/rejet de boutique (backend)", "objective": "Permettre à un admin de faire passer une boutique de `pending` à `active` (ou à `suspended`), flux actuellement inexistant hors du parcours de candidature.", "files": "`server-sanhia/routes/admin.routes.js`", "instructions": "Ajouter `PATCH /api/admin/shops/:id/approve` (passe `status` à `\"active\"`) et `PATCH /api/admin/shops/:id/suspend` (passe `status` à `\"suspended\"`), protégées par `verifyToken` + vérification admin. Suivre le pattern déjà utilisé pour l'approbation des candidatures (`admin.routes.js`, section candidatures) : vérifier que la boutique existe, répondre 404 sinon, répondre la boutique mise à jour en cas de succès.", "validation": "`PATCH /api/admin/shops/:id/approve` sur une boutique `pending` renvoie 200 avec `status: \"active\"` dans le corps ; un compte non-admin reçoit 403 sur cette même route.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre à un admin de faire passer une boutique de `pending` à `active` (ou à `suspended`), flux actuellement inexistant hors du parcours de candidature.\n- **Fichiers impactés :** `server-sanhia/routes/admin.routes.js`\n- **Instructions d'exécution :** Ajouter `PATCH /api/admin/shops/:id/approve` (passe `status` à `\"active\"`) et `PATCH /api/admin/shops/:id/suspend` (passe `status` à `\"suspended\"`), protégées par `verifyToken` + vérification admin. Suivre le pattern déjà utilisé pour l'approbation des candidatures (`admin.routes.js`, section candidatures) : vérifier que la boutique existe, répondre 404 sinon, répondre la boutique mise à jour en cas de succès.\n- **Critères de validation :** `PATCH /api/admin/shops/:id/approve` sur une boutique `pending` renvoie 200 avec `status: \"active\"` dans le corps ; un compte non-admin reçoit 403 sur cette même route.\n- **Statut :** [ ] Non commencé"}, {"id": 13, "title": "Brancher l'approbation boutique dans le back-office admin (web)", "objective": "Rendre la nouvelle route utilisable depuis l'interface admin.", "files": "`sanhia-web/src/pages/Admin.jsx`", "instructions": "Dans l'onglet \"Boutiques\", ajouter un bouton \"Approuver\" (visible si `status === 'pending'`) et \"Suspendre\" (visible si `status === 'active'`) sur chaque ligne, appelant respectivement `PATCH /api/admin/shops/:id/approve` et `.../suspend`, suivant le pattern déjà utilisé pour `validatePayment`/`rejectPayment`.", "validation": "Depuis `/admin` → onglet Boutiques, cliquer \"Approuver\" sur une boutique `pending` la fait passer visuellement à `active` sans recharger la page.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre la nouvelle route utilisable depuis l'interface admin.\n- **Fichiers impactés :** `sanhia-web/src/pages/Admin.jsx`\n- **Instructions d'exécution :** Dans l'onglet \"Boutiques\", ajouter un bouton \"Approuver\" (visible si `status === 'pending'`) et \"Suspendre\" (visible si `status === 'active'`) sur chaque ligne, appelant respectivement `PATCH /api/admin/shops/:id/approve` et `.../suspend`, suivant le pattern déjà utilisé pour `validatePayment`/`rejectPayment`.\n- **Critères de validation :** Depuis `/admin` → onglet Boutiques, cliquer \"Approuver\" sur une boutique `pending` la fait passer visuellement à `active` sans recharger la page.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "D", "name": "Wishlist / Like — réconciliation web", "intro": "> Trois mécanismes déconnectés à unifier. La page `/wishlist` web est actuellement vide pour tous les utilisateurs, tout le temps.", "steps": [{"id": 14, "title": "Créer un `WishlistContext.jsx` partagé côté web", "objective": "Donner au web un mécanisme de favoris persistant unique, sur le modèle du contexte déjà existant côté mobile (`sanhia-mobile/src/context/WishlistContext.js`).", "files": "`sanhia-web/src/context/WishlistContext.jsx` (nouveau), `sanhia-web/src/App.jsx`", "instructions": "Créer le contexte avec les mêmes opérations que la version mobile (`toggle`, `isWishlisted`, `items`), persistance `localStorage` sous la clé `sanhia_wishlist` (clé déjà utilisée par `Wishlist.jsx`, à réutiliser pour compatibilité). Monter `<WishlistProvider>` dans `App.jsx` aux côtés de `<CartProvider>`.", "validation": "Un composant de test appelant `useWishlist().toggle({id:'x', name:'Test', price:1000})` puis `useWishlist().items` renvoie bien un tableau contenant l'article, et `localStorage.getItem('sanhia_wishlist')` reflète le même contenu.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Donner au web un mécanisme de favoris persistant unique, sur le modèle du contexte déjà existant côté mobile (`sanhia-mobile/src/context/WishlistContext.js`).\n- **Fichiers impactés :** `sanhia-web/src/context/WishlistContext.jsx` (nouveau), `sanhia-web/src/App.jsx`\n- **Instructions d'exécution :** Créer le contexte avec les mêmes opérations que la version mobile (`toggle`, `isWishlisted`, `items`), persistance `localStorage` sous la clé `sanhia_wishlist` (clé déjà utilisée par `Wishlist.jsx`, à réutiliser pour compatibilité). Monter `<WishlistProvider>` dans `App.jsx` aux côtés de `<CartProvider>`.\n- **Critères de validation :** Un composant de test appelant `useWishlist().toggle({id:'x', name:'Test', price:1000})` puis `useWishlist().items` renvoie bien un tableau contenant l'article, et `localStorage.getItem('sanhia_wishlist')` reflète le même contenu.\n- **Statut :** [ ] Non commencé"}, {"id": 15, "title": "Brancher le bouton cœur de `Home.jsx` sur la vraie wishlist", "objective": "Remplacer l'état local factice (`setWished`) par un appel réel au contexte partagé.", "files": "`sanhia-web/src/pages/Home.jsx`", "instructions": "Importer `useWishlist` depuis le nouveau contexte. Remplacer `const [wished, setWished] = useState(false)` par `const { isWishlisted, toggle } = useWishlist()`, `wished = isWishlisted(product.id)`, et l'`onClick` du bouton par `toggle(product)`.", "validation": "Cliquer le cœur d'une carte produit sur `/` puis naviguer vers `/wishlist` fait apparaître ce produit dans la liste.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Remplacer l'état local factice (`setWished`) par un appel réel au contexte partagé.\n- **Fichiers impactés :** `sanhia-web/src/pages/Home.jsx`\n- **Instructions d'exécution :** Importer `useWishlist` depuis le nouveau contexte. Remplacer `const [wished, setWished] = useState(false)` par `const { isWishlisted, toggle } = useWishlist()`, `wished = isWishlisted(product.id)`, et l'`onClick` du bouton par `toggle(product)`.\n- **Critères de validation :** Cliquer le cœur d'une carte produit sur `/` puis naviguer vers `/wishlist` fait apparaître ce produit dans la liste.\n- **Statut :** [ ] Non commencé"}, {"id": 16, "title": "Brancher le bouton cœur de `Catalogue.jsx` sur la vraie wishlist", "objective": "Même correctif que l'Étape 15, sur la deuxième surface de découverte.", "files": "`sanhia-web/src/pages/Catalogue.jsx`", "instructions": "Identique à l'Étape 15, appliqué au bouton `prd-wish` de `Catalogue.jsx`.", "validation": "Cliquer le cœur d'une carte produit sur `/catalogue` fait apparaître ce produit dans `/wishlist`.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Même correctif que l'Étape 15, sur la deuxième surface de découverte.\n- **Fichiers impactés :** `sanhia-web/src/pages/Catalogue.jsx`\n- **Instructions d'exécution :** Identique à l'Étape 15, appliqué au bouton `prd-wish` de `Catalogue.jsx`.\n- **Critères de validation :** Cliquer le cœur d'une carte produit sur `/catalogue` fait apparaître ce produit dans `/wishlist`.\n- **Statut :** [ ] Non commencé"}, {"id": 17, "title": "Faire lire `Wishlist.jsx` depuis le contexte partagé", "objective": "Remplacer la lecture/écriture directe de `localStorage` dans `Wishlist.jsx` par le contexte, pour une seule source de vérité.", "files": "`sanhia-web/src/pages/Wishlist.jsx`", "instructions": "Remplacer le `useState(() => JSON.parse(localStorage.getItem('sanhia_wishlist')) || [])` et son `useEffect` de synchronisation par `const { items, toggle } = useWishlist()`. Adapter `onRemove` pour appeler `toggle(item)` (retire l'article s'il est déjà présent).", "validation": "Ajouter un produit à la wishlist depuis `/catalogue`, le retirer depuis `/wishlist`, revenir sur `/catalogue` : le cœur du produit n'est plus actif.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Remplacer la lecture/écriture directe de `localStorage` dans `Wishlist.jsx` par le contexte, pour une seule source de vérité.\n- **Fichiers impactés :** `sanhia-web/src/pages/Wishlist.jsx`\n- **Instructions d'exécution :** Remplacer le `useState(() => JSON.parse(localStorage.getItem('sanhia_wishlist')) || [])` et son `useEffect` de synchronisation par `const { items, toggle } = useWishlist()`. Adapter `onRemove` pour appeler `toggle(item)` (retire l'article s'il est déjà présent).\n- **Critères de validation :** Ajouter un produit à la wishlist depuis `/catalogue`, le retirer depuis `/wishlist`, revenir sur `/catalogue` : le cœur du produit n'est plus actif.\n- **Statut :** [ ] Non commencé"}, {"id": 18, "title": "Porter le Like sur la fiche produit mobile", "objective": "Combler l'écart mobile vs web sur le Like (distinct de la wishlist, déjà fonctionnel côté mobile).", "files": "`sanhia-mobile/app/product/[id].js`", "instructions": "Ajouter un bouton (icône `heart`/cœur avec compteur, distinct visuellement du bouton wishlist déjà présent) appelant `POST /api/products/:id/like` avec le token d'auth, sur le modèle du bouton `ax-bwish` de `sanhia-web/src/pages/Product.jsx`. Afficher `likesCount` retourné par `GET /api/products/:id`.", "validation": "Taper sur le bouton Like d'une fiche produit sur mobile incrémente le compteur affiché et l'appel réseau `POST /products/:id/like` renvoie 200 (vérifiable dans le réseau du dev-client).", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Combler l'écart mobile vs web sur le Like (distinct de la wishlist, déjà fonctionnel côté mobile).\n- **Fichiers impactés :** `sanhia-mobile/app/product/[id].js`\n- **Instructions d'exécution :** Ajouter un bouton (icône `heart`/cœur avec compteur, distinct visuellement du bouton wishlist déjà présent) appelant `POST /api/products/:id/like` avec le token d'auth, sur le modèle du bouton `ax-bwish` de `sanhia-web/src/pages/Product.jsx`. Afficher `likesCount` retourné par `GET /api/products/:id`.\n- **Critères de validation :** Taper sur le bouton Like d'une fiche produit sur mobile incrémente le compteur affiché et l'appel réseau `POST /products/:id/like` renvoie 200 (vérifiable dans le réseau du dev-client).\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "E", "name": "Avis produit (schéma → backend → web → mobile)", "intro": "> La collecte d'avis fonctionne déjà (web) ; cette phase répare la restitution et étend le modèle pour noter un produit précis, pas seulement une boutique.", "steps": [{"id": 19, "title": "Ajouter `productId` optionnel au modèle `Review` (migration Prisma)", "objective": "Permettre de rattacher un avis à un produit précis, en plus de la boutique.", "files": "`server-sanhia/prisma/schema.prisma`, nouvelle migration sous `server-sanhia/prisma/migrations/`", "instructions": "Ajouter au modèle `Review` : `productId String?` et la relation `product Product? @relation(fields: [productId], references: [id])`. Ajouter la relation inverse `reviews Review[]` sur `Product`. Lancer `npx prisma migrate dev --name add_review_product_id`.", "validation": "`npx prisma migrate status` ne montre aucune migration en attente ; `npx prisma studio` (ou une requête directe) confirme la colonne `productId` nullable sur la table `Review`.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre de rattacher un avis à un produit précis, en plus de la boutique.\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`, nouvelle migration sous `server-sanhia/prisma/migrations/`\n- **Instructions d'exécution :** Ajouter au modèle `Review` : `productId String?` et la relation `product Product? @relation(fields: [productId], references: [id])`. Ajouter la relation inverse `reviews Review[]` sur `Product`. Lancer `npx prisma migrate dev --name add_review_product_id`.\n- **Critères de validation :** `npx prisma migrate status` ne montre aucune migration en attente ; `npx prisma studio` (ou une requête directe) confirme la colonne `productId` nullable sur la table `Review`.\n- **Statut :** [ ] Non commencé"}, {"id": 20, "title": "Adapter `POST /api/orders/:id/review` pour accepter un `productId` optionnel", "objective": "Permettre au client de noter un produit précis de sa commande, pas uniquement la boutique globale.", "files": "`server-sanhia/routes/orders.js`", "instructions": "Étendre `reviewSchema` (Zod) avec `productId: z.string().cuid().optional()`. Si fourni, vérifier qu'il correspond bien à un produit de la commande concernée (sinon 400). Inclure `productId` dans le `data` du `prisma.review.create`.", "validation": "`POST /api/orders/:id/review` avec un `productId` valide de la commande crée un avis avec ce `productId` renseigné en base ; avec un `productId` n'appartenant pas à la commande, renvoie 400.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre au client de noter un produit précis de sa commande, pas uniquement la boutique globale.\n- **Fichiers impactés :** `server-sanhia/routes/orders.js`\n- **Instructions d'exécution :** Étendre `reviewSchema` (Zod) avec `productId: z.string().cuid().optional()`. Si fourni, vérifier qu'il correspond bien à un produit de la commande concernée (sinon 400). Inclure `productId` dans le `data` du `prisma.review.create`.\n- **Critères de validation :** `POST /api/orders/:id/review` avec un `productId` valide de la commande crée un avis avec ce `productId` renseigné en base ; avec un `productId` n'appartenant pas à la commande, renvoie 400.\n- **Statut :** [ ] Non commencé"}, {"id": 21, "title": "Inclure les avis dans la réponse `GET /api/shops/:slug`", "objective": "Exposer les avis existants pour que le frontend puisse enfin les afficher.", "files": "`server-sanhia/routes/shops.routes.js`", "instructions": "Ajouter à la requête Prisma de `GET /:slug` un `include: { reviews: { include: { user: { select: { displayName: true } } }, orderBy: { createdAt: 'desc' }, take: 20 } }` (ou équivalent selon la structure de select existante).", "validation": "`GET /api/shops/:slug` sur une boutique ayant au moins un avis renvoie un tableau `reviews` non vide dans la réponse JSON.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Exposer les avis existants pour que le frontend puisse enfin les afficher.\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js`\n- **Instructions d'exécution :** Ajouter à la requête Prisma de `GET /:slug` un `include: { reviews: { include: { user: { select: { displayName: true } } }, orderBy: { createdAt: 'desc' }, take: 20 } }` (ou équivalent selon la structure de select existante).\n- **Critères de validation :** `GET /api/shops/:slug` sur une boutique ayant au moins un avis renvoie un tableau `reviews` non vide dans la réponse JSON.\n- **Statut :** [ ] Non commencé"}, {"id": 22, "title": "Inclure les avis liés dans `GET /api/products/:id`", "objective": "Exposer les avis rattachés spécifiquement à un produit.", "files": "`server-sanhia/routes/products.js`", "instructions": "Ajouter à la requête de `GET /:id` un `include: { likes: ..., reviews: { where: { productId: <id implicite via la relation> }, include: { user: { select: { displayName: true } } } } }` — utiliser la relation inverse ajoutée à l'Étape 19.", "validation": "`GET /api/products/:id` sur un produit ayant reçu un avis avec `productId` renseigné renvoie ce même avis dans la réponse.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Exposer les avis rattachés spécifiquement à un produit.\n- **Fichiers impactés :** `server-sanhia/routes/products.js`\n- **Instructions d'exécution :** Ajouter à la requête de `GET /:id` un `include: { likes: ..., reviews: { where: { productId: <id implicite via la relation> }, include: { user: { select: { displayName: true } } } } }` — utiliser la relation inverse ajoutée à l'Étape 19.\n- **Critères de validation :** `GET /api/products/:id` sur un produit ayant reçu un avis avec `productId` renseigné renvoie ce même avis dans la réponse.\n- **Statut :** [ ] Non commencé"}, {"id": 23, "title": "Brancher `ReviewsSection.jsx` sur les vrais avis (Boutique.jsx web)", "objective": "Afficher enfin les avis collectés au lieu du tableau vide codé en dur.", "files": "`sanhia-web/src/pages/Boutique.jsx`, `sanhia-web/src/components/boutique/ReviewsSection.jsx`", "instructions": "Remplacer `reviews={[]}` par `reviews={shop.reviews || []}` (le champ ajouté à l'Étape 21), en s'assurant que `shop` provient bien de l'appel à `GET /api/shops/:slug`.", "validation": "La page `/boutique/:id` d'une boutique ayant reçu au moins un avis affiche ce commentaire et sa note dans la section Avis, plus le tableau vide par défaut.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Afficher enfin les avis collectés au lieu du tableau vide codé en dur.\n- **Fichiers impactés :** `sanhia-web/src/pages/Boutique.jsx`, `sanhia-web/src/components/boutique/ReviewsSection.jsx`\n- **Instructions d'exécution :** Remplacer `reviews={[]}` par `reviews={shop.reviews || []}` (le champ ajouté à l'Étape 21), en s'assurant que `shop` provient bien de l'appel à `GET /api/shops/:slug`.\n- **Critères de validation :** La page `/boutique/:id` d'une boutique ayant reçu au moins un avis affiche ce commentaire et sa note dans la section Avis, plus le tableau vide par défaut.\n- **Statut :** [ ] Non commencé"}, {"id": 24, "title": "Afficher les avis sur la fiche produit web", "objective": "Remplacer l'onglet Avis factice (state local, jamais persisté) par les vrais avis du produit.", "files": "`sanhia-web/src/pages/Product.jsx`", "instructions": "Supprimer le `handleReview` qui fait `setReviews(prev => [newReview, ...prev])` sans appel réseau. Initialiser `reviews` depuis `product.reviews` (champ ajouté à l'Étape 22) au lieu de `useState([])`. Retirer le formulaire de soumission d'avis de cette page si aucune route de soumission dédiée au produit n'existe (la soumission reste sur le flux post-livraison de `Profile.jsx`) — ou rediriger l'utilisateur vers ce flux.", "validation": "La fiche produit d'un article ayant reçu un avis (via l'Étape 20 avec `productId`) affiche cet avis sans qu'aucune saisie manuelle ne soit nécessaire sur cette page.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Remplacer l'onglet Avis factice (state local, jamais persisté) par les vrais avis du produit.\n- **Fichiers impactés :** `sanhia-web/src/pages/Product.jsx`\n- **Instructions d'exécution :** Supprimer le `handleReview` qui fait `setReviews(prev => [newReview, ...prev])` sans appel réseau. Initialiser `reviews` depuis `product.reviews` (champ ajouté à l'Étape 22) au lieu de `useState([])`. Retirer le formulaire de soumission d'avis de cette page si aucune route de soumission dédiée au produit n'existe (la soumission reste sur le flux post-livraison de `Profile.jsx`) — ou rediriger l'utilisateur vers ce flux.\n- **Critères de validation :** La fiche produit d'un article ayant reçu un avis (via l'Étape 20 avec `productId`) affiche cet avis sans qu'aucune saisie manuelle ne soit nécessaire sur cette page.\n- **Statut :** [ ] Non commencé"}, {"id": 25, "title": "Ajouter un flux de notation post-livraison sur mobile", "objective": "Combler l'absence totale de notation côté mobile.", "files": "`sanhia-mobile/app/(buyer)/profile.js`, nouveau composant `sanhia-mobile/src/components/ReviewModal.js`", "instructions": "Créer un composant modal simple (5 étoiles tap-to-select + champ commentaire optionnel) sur le modèle du `ReviewModal` web (`sanhia-web/src/pages/Profile.jsx`). Dans la liste des commandes de `profile.js`, afficher un bouton \"Noter\" sur les commandes au statut `DELIVERED` sans avis existant, ouvrant la modal et appelant `POST /api/orders/:id/review`.", "validation": "Depuis Profil → Commandes (mobile), noter une commande livrée fait disparaître le bouton \"Noter\" et l'avis est visible via `GET /api/shops/:slug` (vérifiable côté web ou par appel direct).", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Combler l'absence totale de notation côté mobile.\n- **Fichiers impactés :** `sanhia-mobile/app/(buyer)/profile.js`, nouveau composant `sanhia-mobile/src/components/ReviewModal.js`\n- **Instructions d'exécution :** Créer un composant modal simple (5 étoiles tap-to-select + champ commentaire optionnel) sur le modèle du `ReviewModal` web (`sanhia-web/src/pages/Profile.jsx`). Dans la liste des commandes de `profile.js`, afficher un bouton \"Noter\" sur les commandes au statut `DELIVERED` sans avis existant, ouvrant la modal et appelant `POST /api/orders/:id/review`.\n- **Critères de validation :** Depuis Profil → Commandes (mobile), noter une commande livrée fait disparaître le bouton \"Noter\" et l'avis est visible via `GET /api/shops/:slug` (vérifiable côté web ou par appel direct).\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "F", "name": "Recherche boutiques", "intro": "", "steps": [{"id": 26, "title": "Ajouter la recherche texte à `GET /api/shops` (backend)", "objective": "Permettre de rechercher une boutique par mot-clé, actuellement impossible (seul le filtre catégorie existe).", "files": "`server-sanhia/routes/shops.routes.js`", "instructions": "Ajouter un paramètre `q` à `GET /`, filtrant sur `name` (et `description` si pertinent) via `contains` insensible à la casse (`mode: 'insensitive'`), suivant le pattern déjà utilisé dans `products.js` pour son propre paramètre `q`.", "validation": "`GET /api/shops?q=<terme présent dans le nom d'une boutique existante>` renvoie uniquement les boutiques correspondantes ; `GET /api/shops` sans `q` renvoie le comportement inchangé.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre de rechercher une boutique par mot-clé, actuellement impossible (seul le filtre catégorie existe).\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js`\n- **Instructions d'exécution :** Ajouter un paramètre `q` à `GET /`, filtrant sur `name` (et `description` si pertinent) via `contains` insensible à la casse (`mode: 'insensitive'`), suivant le pattern déjà utilisé dans `products.js` pour son propre paramètre `q`.\n- **Critères de validation :** `GET /api/shops?q=<terme présent dans le nom d'une boutique existante>` renvoie uniquement les boutiques correspondantes ; `GET /api/shops` sans `q` renvoie le comportement inchangé.\n- **Statut :** [ ] Non commencé"}, {"id": 27, "title": "Brancher la recherche boutiques côté web", "objective": "Exposer le nouveau paramètre dans l'UI de la liste des boutiques.", "files": "`sanhia-web/src/pages/BoutiqueList.jsx`", "instructions": "Ajouter un champ de recherche texte au-dessus de la grille de boutiques, passant sa valeur en paramètre `q` à l'appel `GET /api/shops`, avec un léger debounce (300ms) pour éviter une requête par frappe.", "validation": "Taper un mot-clé dans le champ de recherche sur `/boutiques` filtre visuellement la liste sans rechargement de page.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Exposer le nouveau paramètre dans l'UI de la liste des boutiques.\n- **Fichiers impactés :** `sanhia-web/src/pages/BoutiqueList.jsx`\n- **Instructions d'exécution :** Ajouter un champ de recherche texte au-dessus de la grille de boutiques, passant sa valeur en paramètre `q` à l'appel `GET /api/shops`, avec un léger debounce (300ms) pour éviter une requête par frappe.\n- **Critères de validation :** Taper un mot-clé dans le champ de recherche sur `/boutiques` filtre visuellement la liste sans rechargement de page.\n- **Statut :** [ ] Non commencé"}, {"id": 28, "title": "Brancher la recherche boutiques côté mobile", "objective": "Parité avec le web sur l'écran Boutiques.", "files": "`sanhia-mobile/app/(buyer)/boutiques.js`", "instructions": "Ajouter un `TextInput` de recherche au-dessus de la grille, sur le modèle de celui déjà présent dans `catalogue.js`, passant sa valeur en paramètre `q` à la query `GET /api/shops`.", "validation": "Taper un mot-clé dans le nouveau champ sur l'écran Boutiques (mobile) filtre visuellement la grille.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Parité avec le web sur l'écran Boutiques.\n- **Fichiers impactés :** `sanhia-mobile/app/(buyer)/boutiques.js`\n- **Instructions d'exécution :** Ajouter un `TextInput` de recherche au-dessus de la grille, sur le modèle de celui déjà présent dans `catalogue.js`, passant sa valeur en paramètre `q` à la query `GET /api/shops`.\n- **Critères de validation :** Taper un mot-clé dans le nouveau champ sur l'écran Boutiques (mobile) filtre visuellement la grille.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "G", "name": "Vues par produit (schéma → backend → web → mobile)", "intro": "", "steps": [{"id": 29, "title": "Ajouter le champ `views` au modèle `Product` (migration Prisma)", "objective": "Poser la fondation de données pour le comptage de vues par produit, actuellement inexistant.", "files": "`server-sanhia/prisma/schema.prisma`, nouvelle migration", "instructions": "Ajouter `views Int @default(0)` au modèle `Product`, sur le modèle du champ déjà existant sur `Shop`. Lancer `npx prisma migrate dev --name add_product_views`.", "validation": "`npx prisma migrate status` ne montre aucune migration en attente ; le champ `views` apparaît avec la valeur `0` par défaut sur un produit existant.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Poser la fondation de données pour le comptage de vues par produit, actuellement inexistant.\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`, nouvelle migration\n- **Instructions d'exécution :** Ajouter `views Int @default(0)` au modèle `Product`, sur le modèle du champ déjà existant sur `Shop`. Lancer `npx prisma migrate dev --name add_product_views`.\n- **Critères de validation :** `npx prisma migrate status` ne montre aucune migration en attente ; le champ `views` apparaît avec la valeur `0` par défaut sur un produit existant.\n- **Statut :** [ ] Non commencé"}, {"id": 30, "title": "Incrémenter `Product.views` sur `GET /api/products/:id`", "objective": "Faire vivre le compteur ajouté à l'Étape 29.", "files": "`server-sanhia/routes/products.js`", "instructions": "Dans le handler `GET /:id`, après avoir récupéré le produit, ajouter un `prisma.product.update({ where: { id }, data: { views: { increment: 1 } } })` en fire-and-forget (ne pas bloquer la réponse dessus), sur le modèle de l'incrément déjà en place pour `Shop.views` dans `shops.routes.js`.", "validation": "Deux appels successifs à `GET /api/products/:id` sur le même produit font passer `views` de `N` à `N+2` (vérifiable via une troisième lecture ou Prisma Studio).", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Faire vivre le compteur ajouté à l'Étape 29.\n- **Fichiers impactés :** `server-sanhia/routes/products.js`\n- **Instructions d'exécution :** Dans le handler `GET /:id`, après avoir récupéré le produit, ajouter un `prisma.product.update({ where: { id }, data: { views: { increment: 1 } } })` en fire-and-forget (ne pas bloquer la réponse dessus), sur le modèle de l'incrément déjà en place pour `Shop.views` dans `shops.routes.js`.\n- **Critères de validation :** Deux appels successifs à `GET /api/products/:id` sur le même produit font passer `views` de `N` à `N+2` (vérifiable via une troisième lecture ou Prisma Studio).\n- **Statut :** [ ] Non commencé"}, {"id": 31, "title": "Afficher le compteur de vues sur la fiche produit web", "objective": "Rendre visible la donnée désormais collectée.", "files": "`sanhia-web/src/pages/Product.jsx`", "instructions": "Afficher `product.views` (icône œil + nombre) près du prix ou des métadonnées produit.", "validation": "La fiche produit affiche un nombre de vues cohérent avec le compteur backend, incrémenté à chaque visite.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre visible la donnée désormais collectée.\n- **Fichiers impactés :** `sanhia-web/src/pages/Product.jsx`\n- **Instructions d'exécution :** Afficher `product.views` (icône œil + nombre) près du prix ou des métadonnées produit.\n- **Critères de validation :** La fiche produit affiche un nombre de vues cohérent avec le compteur backend, incrémenté à chaque visite.\n- **Statut :** [ ] Non commencé"}, {"id": 32, "title": "Afficher les vues cumulées par produit dans le dashboard vendeur web", "objective": "Donner au vendeur une vue produit par produit, en complément du total boutique déjà affiché.", "files": "`sanhia-web/src/pages/Seller.jsx` (onglet Produits ou Vue d'ensemble)", "instructions": "Dans la liste \"Mes Produits\", ajouter une colonne/valeur \"Vues\" par ligne, sourcée depuis `product.views` (déjà inclus dans `GET /products/mine` si la route renvoie les champs scalaires du produit sans `select` restrictif — vérifier et ajuster si besoin).", "validation": "Chaque ligne de la liste \"Mes Produits\" affiche un nombre de vues correspondant aux consultations réelles de la fiche produit.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Donner au vendeur une vue produit par produit, en complément du total boutique déjà affiché.\n- **Fichiers impactés :** `sanhia-web/src/pages/Seller.jsx` (onglet Produits ou Vue d'ensemble)\n- **Instructions d'exécution :** Dans la liste \"Mes Produits\", ajouter une colonne/valeur \"Vues\" par ligne, sourcée depuis `product.views` (déjà inclus dans `GET /products/mine` si la route renvoie les champs scalaires du produit sans `select` restrictif — vérifier et ajuster si besoin).\n- **Critères de validation :** Chaque ligne de la liste \"Mes Produits\" affiche un nombre de vues correspondant aux consultations réelles de la fiche produit.\n- **Statut :** [ ] Non commencé"}, {"id": 33, "title": "Afficher les vues cumulées par produit dans le dashboard vendeur mobile", "objective": "Parité avec le web.", "files": "`sanhia-mobile/app/(seller)/products.js`", "instructions": "Ajouter l'affichage de `product.views` sur chaque ligne de `ProductRow`, à côté du stock déjà affiché.", "validation": "L'écran Produits (vendeur, mobile) affiche un nombre de vues par produit cohérent avec les consultations réelles.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Parité avec le web.\n- **Fichiers impactés :** `sanhia-mobile/app/(seller)/products.js`\n- **Instructions d'exécution :** Ajouter l'affichage de `product.views` sur chaque ligne de `ProductRow`, à côté du stock déjà affiché.\n- **Critères de validation :** L'écran Produits (vendeur, mobile) affiche un nombre de vues par produit cohérent avec les consultations réelles.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "H", "name": "Upload photo produit (mobile)", "intro": "", "steps": [{"id": 34, "title": "Installer et configurer `expo-image-picker` pour l'ajout de produit", "objective": "Rendre disponible la brique de sélection photo sur l'écran d'ajout de produit (elle est déjà installée et utilisée pour la candidature vendeur, mais pas branchée ici).", "files": "`sanhia-mobile/app/seller/add-product.js`", "instructions": "Importer `* as ImagePicker from 'expo-image-picker'` (déjà une dépendance du projet, pas de nouvelle installation nécessaire — vérifier via `sanhia-mobile/package.json`). Ajouter un state `images` (tableau d'assets sélectionnés) et une fonction `pickImages` appelant `ImagePicker.launchImageLibraryAsync({ mediaTypes: ['images'], allowsMultipleSelection: true, quality: 0.8 })`, sur le modèle de `pickFile` dans `app/seller-application.js`.", "validation": "Taper sur un nouveau bouton \"Ajouter des photos\" ouvre la galerie du téléphone et permet de sélectionner plusieurs images, stockées dans le state local.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre disponible la brique de sélection photo sur l'écran d'ajout de produit (elle est déjà installée et utilisée pour la candidature vendeur, mais pas branchée ici).\n- **Fichiers impactés :** `sanhia-mobile/app/seller/add-product.js`\n- **Instructions d'exécution :** Importer `* as ImagePicker from 'expo-image-picker'` (déjà une dépendance du projet, pas de nouvelle installation nécessaire — vérifier via `sanhia-mobile/package.json`). Ajouter un state `images` (tableau d'assets sélectionnés) et une fonction `pickImages` appelant `ImagePicker.launchImageLibraryAsync({ mediaTypes: ['images'], allowsMultipleSelection: true, quality: 0.8 })`, sur le modèle de `pickFile` dans `app/seller-application.js`.\n- **Critères de validation :** Taper sur un nouveau bouton \"Ajouter des photos\" ouvre la galerie du téléphone et permet de sélectionner plusieurs images, stockées dans le state local.\n- **Statut :** [ ] Non commencé"}, {"id": 35, "title": "Ajouter la prévisualisation des photos sélectionnées à l'écran", "objective": "Donner un retour visuel avant soumission.", "files": "`sanhia-mobile/app/seller/add-product.js`", "instructions": "Afficher une rangée de vignettes (`Image` RN, ~70x70) pour chaque photo sélectionnée, avec un bouton de suppression individuel par vignette.", "validation": "Sélectionner 3 photos affiche 3 vignettes distinctes ; retirer l'une d'elles la fait disparaître sans affecter les deux autres.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Donner un retour visuel avant soumission.\n- **Fichiers impactés :** `sanhia-mobile/app/seller/add-product.js`\n- **Instructions d'exécution :** Afficher une rangée de vignettes (`Image` RN, ~70x70) pour chaque photo sélectionnée, avec un bouton de suppression individuel par vignette.\n- **Critères de validation :** Sélectionner 3 photos affiche 3 vignettes distinctes ; retirer l'une d'elles la fait disparaître sans affecter les deux autres.\n- **Statut :** [ ] Non commencé"}, {"id": 36, "title": "Envoyer les photos sélectionnées à `POST /api/products`", "objective": "Compléter la soumission du formulaire pour inclure les fichiers, le backend acceptant déjà des images (route déjà utilisée par le web).", "files": "`sanhia-mobile/app/seller/add-product.js`", "instructions": "Dans `handleSubmit`, ajouter chaque image du state à l'objet `FormData` existant via `body.append('images', { uri: asset.uri, name: asset.fileName || 'photo.jpg', type: asset.mimeType || 'image/jpeg' })`, sur le modèle exact du multipart déjà utilisé dans `app/seller-application.js`. Ne pas fixer de `Content-Type` manuel (même piège que Phase A).", "validation": "Publier un produit avec au moins une photo depuis le mobile fait apparaître ce produit, avec son image, dans \"Mes produits\" (mobile) et sur la fiche produit consultée depuis le web.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Compléter la soumission du formulaire pour inclure les fichiers, le backend acceptant déjà des images (route déjà utilisée par le web).\n- **Fichiers impactés :** `sanhia-mobile/app/seller/add-product.js`\n- **Instructions d'exécution :** Dans `handleSubmit`, ajouter chaque image du state à l'objet `FormData` existant via `body.append('images', { uri: asset.uri, name: asset.fileName || 'photo.jpg', type: asset.mimeType || 'image/jpeg' })`, sur le modèle exact du multipart déjà utilisé dans `app/seller-application.js`. Ne pas fixer de `Content-Type` manuel (même piège que Phase A).\n- **Critères de validation :** Publier un produit avec au moins une photo depuis le mobile fait apparaître ce produit, avec son image, dans \"Mes produits\" (mobile) et sur la fiche produit consultée depuis le web.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "I", "name": "Statuts de livraison intermédiaires", "intro": "", "steps": [{"id": 37, "title": "Exposer la transition `PICKED_UP` côté backend", "objective": "Permettre au livreur de signaler la collecte du colis, distincte de l'acceptation de la mission.", "files": "`server-sanhia/routes/delivery.routes.js`", "instructions": "Ajouter `PATCH /api/delivery/:id/pickup`, passant `Livraison.statut` de `ACCEPTED` à `PICKED_UP` (rejeter avec 400 si le statut actuel n'est pas `ACCEPTED`), protégée par `verifyDelivery` et vérification que la mission appartient bien au livreur connecté.", "validation": "`PATCH /api/delivery/:id/pickup` sur une mission `ACCEPTED` renvoie 200 avec le nouveau statut ; la même requête sur une mission déjà `PICKED_UP` renvoie 400.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre au livreur de signaler la collecte du colis, distincte de l'acceptation de la mission.\n- **Fichiers impactés :** `server-sanhia/routes/delivery.routes.js`\n- **Instructions d'exécution :** Ajouter `PATCH /api/delivery/:id/pickup`, passant `Livraison.statut` de `ACCEPTED` à `PICKED_UP` (rejeter avec 400 si le statut actuel n'est pas `ACCEPTED`), protégée par `verifyDelivery` et vérification que la mission appartient bien au livreur connecté.\n- **Critères de validation :** `PATCH /api/delivery/:id/pickup` sur une mission `ACCEPTED` renvoie 200 avec le nouveau statut ; la même requête sur une mission déjà `PICKED_UP` renvoie 400.\n- **Statut :** [ ] Non commencé"}, {"id": 38, "title": "Exposer les transitions `IN_TRANSIT` et `FAILED` côté backend", "objective": "Compléter le cycle de vie d'une livraison avec le statut \"en route\" et un chemin d'échec explicite.", "files": "`server-sanhia/routes/delivery.routes.js`", "instructions": "Ajouter `PATCH /api/delivery/:id/in-transit` (`PICKED_UP` → `IN_TRANSIT`) et `PATCH /api/delivery/:id/fail` (accepte un `reason` optionnel dans le body, passe le statut à `FAILED` depuis n'importe quel statut actif), même pattern de garde que l'Étape 37.", "validation": "Les deux nouvelles routes respectent les transitions autorisées (400 si transition invalide) et renvoient 200 avec le nouveau statut sinon.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Compléter le cycle de vie d'une livraison avec le statut \"en route\" et un chemin d'échec explicite.\n- **Fichiers impactés :** `server-sanhia/routes/delivery.routes.js`\n- **Instructions d'exécution :** Ajouter `PATCH /api/delivery/:id/in-transit` (`PICKED_UP` → `IN_TRANSIT`) et `PATCH /api/delivery/:id/fail` (accepte un `reason` optionnel dans le body, passe le statut à `FAILED` depuis n'importe quel statut actif), même pattern de garde que l'Étape 37.\n- **Critères de validation :** Les deux nouvelles routes respectent les transitions autorisées (400 si transition invalide) et renvoient 200 avec le nouveau statut sinon.\n- **Statut :** [ ] Non commencé"}, {"id": 39, "title": "Adapter l'écran \"En cours\" web pour les nouveaux statuts", "objective": "Rendre les nouvelles transitions actionnables depuis l'interface livreur.", "files": "`sanhia-web/src/pages/Delivery.jsx`", "instructions": "Remplacer le bouton unique \"Confirmer la livraison\" par une séquence de boutons contextuels selon le statut courant de la mission : \"Colis récupéré\" (si `ACCEPTED`) → \"Confirmer la livraison\" (si `IN_TRANSIT`), plus un lien \"Signaler un problème\" ouvrant une petite modale de motif, appelant les routes de l'Étape 37/38.", "validation": "Une mission acceptée affiche \"Colis récupéré\" ; après ce clic, elle affiche \"Confirmer la livraison\" ; le statut affiché correspond à chaque étape à celui renvoyé par le backend.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre les nouvelles transitions actionnables depuis l'interface livreur.\n- **Fichiers impactés :** `sanhia-web/src/pages/Delivery.jsx`\n- **Instructions d'exécution :** Remplacer le bouton unique \"Confirmer la livraison\" par une séquence de boutons contextuels selon le statut courant de la mission : \"Colis récupéré\" (si `ACCEPTED`) → \"Confirmer la livraison\" (si `IN_TRANSIT`), plus un lien \"Signaler un problème\" ouvrant une petite modale de motif, appelant les routes de l'Étape 37/38.\n- **Critères de validation :** Une mission acceptée affiche \"Colis récupéré\" ; après ce clic, elle affiche \"Confirmer la livraison\" ; le statut affiché correspond à chaque étape à celui renvoyé par le backend.\n- **Statut :** [ ] Non commencé"}, {"id": 40, "title": "Adapter l'écran \"En cours\" mobile pour les nouveaux statuts", "objective": "Parité avec le web.", "files": "`sanhia-mobile/app/(delivery)/active.js`", "instructions": "Même logique qu'à l'Étape 39, adaptée aux composants `Button`/`Alert` déjà utilisés sur cet écran.", "validation": "L'écran \"En cours\" (mobile) permet de suivre la même séquence de statuts que le web, avec le bon libellé de bouton à chaque étape.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Parité avec le web.\n- **Fichiers impactés :** `sanhia-mobile/app/(delivery)/active.js`\n- **Instructions d'exécution :** Même logique qu'à l'Étape 39, adaptée aux composants `Button`/`Alert` déjà utilisés sur cet écran.\n- **Critères de validation :** L'écran \"En cours\" (mobile) permet de suivre la même séquence de statuts que le web, avec le bon libellé de bouton à chaque étape.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "J", "name": "Centre de notifications (historique)", "intro": "", "steps": [{"id": 41, "title": "Ajouter le modèle `Notification` à Prisma (migration)", "objective": "Poser la fondation de données pour un historique de notifications consultable, actuellement inexistant (seul le push éphémère existe).", "files": "`server-sanhia/prisma/schema.prisma`, nouvelle migration", "instructions": "Ajouter un modèle `Notification { id String @id @default(cuid()), userId String, user User @relation(...), title String, body String, link String?, readAt DateTime?, createdAt DateTime @default(now()) }`. Lancer `npx prisma migrate dev --name add_notification_model`.", "validation": "`npx prisma migrate status` ne montre aucune migration en attente ; la table `Notification` existe et est vide après migration.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Poser la fondation de données pour un historique de notifications consultable, actuellement inexistant (seul le push éphémère existe).\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`, nouvelle migration\n- **Instructions d'exécution :** Ajouter un modèle `Notification { id String @id @default(cuid()), userId String, user User @relation(...), title String, body String, link String?, readAt DateTime?, createdAt DateTime @default(now()) }`. Lancer `npx prisma migrate dev --name add_notification_model`.\n- **Critères de validation :** `npx prisma migrate status` ne montre aucune migration en attente ; la table `Notification` existe et est vide après migration.\n- **Statut :** [ ] Non commencé"}, {"id": 42, "title": "Écrire une notification en base à chaque envoi push", "objective": "Faire vivre l'historique en parallèle de l'envoi FCM déjà en place.", "files": "`server-sanhia/services/notifications.js`", "instructions": "Dans `sendPushToUser` (et `sendPushToUsers`/`notifyNewMission`/`notifyNewProduct` si elles n'appellent pas déjà `sendPushToUser` en sous-main), ajouter un `prisma.notification.create({ data: { userId, title, body, link: data?.link } })` avant ou après l'appel FCM — l'écriture en base ne doit pas être bloquée par un éventuel échec d'envoi push (try/catch séparés).", "validation": "Déclencher une notification existante (ex. validation d'un paiement) crée une ligne dans la table `Notification` avec le bon `userId`, en plus du push déjà envoyé.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Faire vivre l'historique en parallèle de l'envoi FCM déjà en place.\n- **Fichiers impactés :** `server-sanhia/services/notifications.js`\n- **Instructions d'exécution :** Dans `sendPushToUser` (et `sendPushToUsers`/`notifyNewMission`/`notifyNewProduct` si elles n'appellent pas déjà `sendPushToUser` en sous-main), ajouter un `prisma.notification.create({ data: { userId, title, body, link: data?.link } })` avant ou après l'appel FCM — l'écriture en base ne doit pas être bloquée par un éventuel échec d'envoi push (try/catch séparés).\n- **Critères de validation :** Déclencher une notification existante (ex. validation d'un paiement) crée une ligne dans la table `Notification` avec le bon `userId`, en plus du push déjà envoyé.\n- **Statut :** [ ] Non commencé"}, {"id": 43, "title": "Ajouter la route `GET /api/notifications`", "objective": "Exposer l'historique au frontend.", "files": "`server-sanhia/routes/` (nouveau fichier `notifications.routes.js`), `server-sanhia/index.js`", "instructions": "Créer la route, protégée par `verifyToken`, renvoyant les notifications de `req.user.uid` triées par `createdAt desc`, paginées (`page`/`limit` sur le modèle de `orders.js`). Monter le router dans `index.js` sous `/api/notifications`.", "validation": "`GET /api/notifications` avec un token valide renvoie les notifications de l'utilisateur connecté, les plus récentes en premier.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Exposer l'historique au frontend.\n- **Fichiers impactés :** `server-sanhia/routes/` (nouveau fichier `notifications.routes.js`), `server-sanhia/index.js`\n- **Instructions d'exécution :** Créer la route, protégée par `verifyToken`, renvoyant les notifications de `req.user.uid` triées par `createdAt desc`, paginées (`page`/`limit` sur le modèle de `orders.js`). Monter le router dans `index.js` sous `/api/notifications`.\n- **Critères de validation :** `GET /api/notifications` avec un token valide renvoie les notifications de l'utilisateur connecté, les plus récentes en premier.\n- **Statut :** [ ] Non commencé"}, {"id": 44, "title": "Ajouter la route `PATCH /api/notifications/:id/read`", "objective": "Permettre de marquer une notification comme lue.", "files": "`server-sanhia/routes/notifications.routes.js`", "instructions": "Ajouter la route, IDOR vérifié (`userId === req.user.uid` avant update), passant `readAt` à `new Date()`.", "validation": "`PATCH /api/notifications/:id/read` sur une notification de l'utilisateur connecté renvoie 200 avec `readAt` renseigné ; sur une notification d'un autre utilisateur, renvoie 403/404.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre de marquer une notification comme lue.\n- **Fichiers impactés :** `server-sanhia/routes/notifications.routes.js`\n- **Instructions d'exécution :** Ajouter la route, IDOR vérifié (`userId === req.user.uid` avant update), passant `readAt` à `new Date()`.\n- **Critères de validation :** `PATCH /api/notifications/:id/read` sur une notification de l'utilisateur connecté renvoie 200 avec `readAt` renseigné ; sur une notification d'un autre utilisateur, renvoie 403/404.\n- **Statut :** [ ] Non commencé"}, {"id": 45, "title": "Créer l'écran Centre de notifications côté web", "objective": "Rendre l'historique consultable dans l'interface.", "files": "`sanhia-web/src/pages/Notifications.jsx` (nouveau), `sanhia-web/src/App.jsx`, `sanhia-web/src/components/layout/Header.jsx`", "instructions": "Créer la page listant les notifications (`GET /api/notifications`), avec un indicateur visuel non-lu/lu et un clic marquant comme lue (`PATCH .../read`) puis naviguant vers `link` si présent. Ajouter la route `/notifications` dans `App.jsx` (protégée, `PrivateRoute`) et une icône cloche avec badge de compteur non-lu dans `Header.jsx`.", "validation": "`/notifications` affiche l'historique réel de l'utilisateur connecté ; cliquer une notification non lue met à jour son état visuel sans recharger la page.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre l'historique consultable dans l'interface.\n- **Fichiers impactés :** `sanhia-web/src/pages/Notifications.jsx` (nouveau), `sanhia-web/src/App.jsx`, `sanhia-web/src/components/layout/Header.jsx`\n- **Instructions d'exécution :** Créer la page listant les notifications (`GET /api/notifications`), avec un indicateur visuel non-lu/lu et un clic marquant comme lue (`PATCH .../read`) puis naviguant vers `link` si présent. Ajouter la route `/notifications` dans `App.jsx` (protégée, `PrivateRoute`) et une icône cloche avec badge de compteur non-lu dans `Header.jsx`.\n- **Critères de validation :** `/notifications` affiche l'historique réel de l'utilisateur connecté ; cliquer une notification non lue met à jour son état visuel sans recharger la page.\n- **Statut :** [ ] Non commencé"}, {"id": 46, "title": "Créer l'écran Centre de notifications côté mobile", "objective": "Parité avec le web.", "files": "`sanhia-mobile/app/notifications.js` (nouveau), point d'entrée dans `app/(buyer)/profile.js` (et éventuellement seller/delivery)", "instructions": "Même logique que l'Étape 45, avec les composants `RowListSkeleton`/`EmptyState` déjà utilisés ailleurs dans le projet mobile pour les états de chargement/vide. Ajouter un lien \"Notifications\" dans les boutons utilitaires de Profil (à côté de \"Messages\").", "validation": "L'écran affiche l'historique réel de l'utilisateur connecté, avec le même comportement de marquage lu qu'au web.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Parité avec le web.\n- **Fichiers impactés :** `sanhia-mobile/app/notifications.js` (nouveau), point d'entrée dans `app/(buyer)/profile.js` (et éventuellement seller/delivery)\n- **Instructions d'exécution :** Même logique que l'Étape 45, avec les composants `RowListSkeleton`/`EmptyState` déjà utilisés ailleurs dans le projet mobile pour les états de chargement/vide. Ajouter un lien \"Notifications\" dans les boutons utilitaires de Profil (à côté de \"Messages\").\n- **Critères de validation :** L'écran affiche l'historique réel de l'utilisateur connecté, avec le même comportement de marquage lu qu'au web.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "K", "name": "Agrégation temporelle du dashboard vendeur", "intro": "", "steps": [{"id": 47, "title": "Ajouter un horodatage aux vues boutique (migration + logique)", "objective": "Permettre un filtrage par période, impossible avec le simple compteur cumulatif actuel.", "files": "`server-sanhia/prisma/schema.prisma`, nouvelle migration, `server-sanhia/routes/shops.routes.js`", "instructions": "Ajouter un modèle léger `ShopView { id String @id @default(cuid()), shopId String, shop Shop @relation(...), createdAt DateTime @default(now()) }`. Dans la route qui incrémente `Shop.views`, ajouter en parallèle un `prisma.shopView.create({ data: { shopId } })`. Garder `Shop.views` tel quel pour compatibilité (compteur cumulatif rapide), la nouvelle table sert uniquement à l'agrégation par période.", "validation": "Après migration, chaque visite d'une boutique crée une ligne `ShopView` horodatée, en plus de l'incrément existant sur `Shop.views`.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre un filtrage par période, impossible avec le simple compteur cumulatif actuel.\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`, nouvelle migration, `server-sanhia/routes/shops.routes.js`\n- **Instructions d'exécution :** Ajouter un modèle léger `ShopView { id String @id @default(cuid()), shopId String, shop Shop @relation(...), createdAt DateTime @default(now()) }`. Dans la route qui incrémente `Shop.views`, ajouter en parallèle un `prisma.shopView.create({ data: { shopId } })`. Garder `Shop.views` tel quel pour compatibilité (compteur cumulatif rapide), la nouvelle table sert uniquement à l'agrégation par période.\n- **Critères de validation :** Après migration, chaque visite d'une boutique crée une ligne `ShopView` horodatée, en plus de l'incrément existant sur `Shop.views`.\n- **Statut :** [ ] Non commencé"}, {"id": 48, "title": "Ajouter le filtre semaine/mois au dashboard vendeur (backend)", "objective": "Exposer l'agrégation par période.", "files": "`server-sanhia/routes/shops.routes.js` (ou nouvelle route dédiée)", "instructions": "Ajouter `GET /api/shops/mine/stats?period=week|month`, comptant les lignes `ShopView` (Étape 47) et les commandes de la boutique dans la fenêtre temporelle demandée (`gte: <date calculée>`).", "validation": "`GET /api/shops/mine/stats?period=week` renvoie des compteurs de vues/commandes cohérents avec les seules 7 derniers jours de données de test.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Exposer l'agrégation par période.\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js` (ou nouvelle route dédiée)\n- **Instructions d'exécution :** Ajouter `GET /api/shops/mine/stats?period=week|month`, comptant les lignes `ShopView` (Étape 47) et les commandes de la boutique dans la fenêtre temporelle demandée (`gte: <date calculée>`).\n- **Critères de validation :** `GET /api/shops/mine/stats?period=week` renvoie des compteurs de vues/commandes cohérents avec les seules 7 derniers jours de données de test.\n- **Statut :** [ ] Non commencé"}, {"id": 49, "title": "Ajouter le sélecteur de période au dashboard vendeur web", "objective": "Rendre le filtre utilisable depuis l'interface.", "files": "`sanhia-web/src/pages/Seller.jsx` (`OverviewTab`)", "instructions": "Ajouter un sélecteur \"Cette semaine / Ce mois\" au-dessus des statistiques, rechargeant les chiffres via l'endpoint de l'Étape 48 au changement de sélection.", "validation": "Changer le sélecteur de période met à jour les chiffres affichés sans recharger la page.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre le filtre utilisable depuis l'interface.\n- **Fichiers impactés :** `sanhia-web/src/pages/Seller.jsx` (`OverviewTab`)\n- **Instructions d'exécution :** Ajouter un sélecteur \"Cette semaine / Ce mois\" au-dessus des statistiques, rechargeant les chiffres via l'endpoint de l'Étape 48 au changement de sélection.\n- **Critères de validation :** Changer le sélecteur de période met à jour les chiffres affichés sans recharger la page.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "L", "name": "Cohérence des réponses API (dette moyenne)", "intro": "", "steps": [{"id": 50, "title": "Harmoniser la pagination de `GET /api/shops`", "objective": "Aligner la forme de réponse de cette route sur le format `{ items, pagination }` déjà utilisé par `GET /api/orders`, pour réduire l'hétérogénéité relevée dans l'audit.", "files": "`server-sanhia/routes/shops.routes.js`, `sanhia-web/src/pages/BoutiqueList.jsx`, `sanhia-mobile/app/(buyer)/boutiques.js`", "instructions": "Remplacer le tableau brut + header `X-Total-Count` par `{ shops: [...], pagination: { page, limit, total, pages } }`. Adapter les deux call sites frontend (web et mobile) qui consomment `GET /api/shops` pour lire cette nouvelle forme.", "validation": "`GET /api/shops` renvoie un objet avec les clés `shops` et `pagination` ; la liste des boutiques s'affiche toujours correctement sur web et mobile après le changement.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Aligner la forme de réponse de cette route sur le format `{ items, pagination }` déjà utilisé par `GET /api/orders`, pour réduire l'hétérogénéité relevée dans l'audit.\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js`, `sanhia-web/src/pages/BoutiqueList.jsx`, `sanhia-mobile/app/(buyer)/boutiques.js`\n- **Instructions d'exécution :** Remplacer le tableau brut + header `X-Total-Count` par `{ shops: [...], pagination: { page, limit, total, pages } }`. Adapter les deux call sites frontend (web et mobile) qui consomment `GET /api/shops` pour lire cette nouvelle forme.\n- **Critères de validation :** `GET /api/shops` renvoie un objet avec les clés `shops` et `pagination` ; la liste des boutiques s'affiche toujours correctement sur web et mobile après le changement.\n- **Statut :** [ ] Non commencé"}, {"id": 51, "title": "Ajouter la pagination aux endpoints de listing admin", "objective": "Éviter de charger des tables entières à chaque appel du back-office, à mesure que le volume de données grossit.", "files": "`server-sanhia/routes/admin.routes.js`", "instructions": "Ajouter `skip`/`take` (calculés depuis des paramètres `page`/`limit`, défaut `limit=50`) à chacun des `findMany` de listing (commandes, boutiques, utilisateurs, produits, candidatures, promos, newsletter, contacts).", "validation": "`GET /api/admin/orders?page=1&limit=10` renvoie au maximum 10 résultats, `page=2` renvoie la page suivante sans chevauchement.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Éviter de charger des tables entières à chaque appel du back-office, à mesure que le volume de données grossit.\n- **Fichiers impactés :** `server-sanhia/routes/admin.routes.js`\n- **Instructions d'exécution :** Ajouter `skip`/`take` (calculés depuis des paramètres `page`/`limit`, défaut `limit=50`) à chacun des `findMany` de listing (commandes, boutiques, utilisateurs, produits, candidatures, promos, newsletter, contacts).\n- **Critères de validation :** `GET /api/admin/orders?page=1&limit=10` renvoie au maximum 10 résultats, `page=2` renvoie la page suivante sans chevauchement.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "M", "name": "Compression média côté serveur", "intro": "", "steps": [{"id": 52, "title": "Compresser les images à l'upload (backend)", "objective": "Garantir la compression quel que soit le point d'entrée, plutôt que de dépendre d'une compression client appliquée de façon inégale.", "files": "`server-sanhia/services/upload.js`, `server-sanhia/package.json`", "instructions": "Ajouter la dépendance `sharp`. Dans `uploadToStorage`, avant l'envoi vers R2, redimensionner l'image (largeur max ~1600px) et recompresser en JPEG qualité ~80 via `sharp(buffer).resize({ width: 1600, withoutEnlargement: true }).jpeg({ quality: 80 }).toBuffer()`, en conservant le buffer d'origine si le fichier n'est pas une image traitable (fallback silencieux, ne jamais bloquer un upload).", "validation": "Uploader une image de plus de 3 Mo aboutit à un fichier stocké sur R2 significativement plus léger (vérifiable via la taille du fichier retourné par l'URL publique).", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Garantir la compression quel que soit le point d'entrée, plutôt que de dépendre d'une compression client appliquée de façon inégale.\n- **Fichiers impactés :** `server-sanhia/services/upload.js`, `server-sanhia/package.json`\n- **Instructions d'exécution :** Ajouter la dépendance `sharp`. Dans `uploadToStorage`, avant l'envoi vers R2, redimensionner l'image (largeur max ~1600px) et recompresser en JPEG qualité ~80 via `sharp(buffer).resize({ width: 1600, withoutEnlargement: true }).jpeg({ quality: 80 }).toBuffer()`, en conservant le buffer d'origine si le fichier n'est pas une image traitable (fallback silencieux, ne jamais bloquer un upload).\n- **Critères de validation :** Uploader une image de plus de 3 Mo aboutit à un fichier stocké sur R2 significativement plus léger (vérifiable via la taille du fichier retourné par l'URL publique).\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "N", "name": "Fil de découverte social (décision produit requise avant exécution)", "intro": "> ⚠️ Cette phase est le chantier le plus lourd de la roadmap et touche au cœur de la proposition de valeur produit (voir audit, \"écart de vision majeur\"). Ne pas exécuter les étapes 54+ sans validation explicite de l'Étape 53 par le porteur produit — c'est un choix délibéré, pas un oubli technique à corriger mécaniquement.", "steps": [{"id": 53, "title": "Valider la spec fonctionnelle du fil avec le porteur produit", "objective": "Trancher consciemment entre \"garder la grille catalogue comme interprétation MVP pragmatique\" et \"construire le fil vertical décrit dans le cahier des charges\", avant d'engager du temps de développement dessus.", "files": "aucun (décision produit, pas de code)", "instructions": "Documenter dans ce fichier (note à ajouter sous cette étape) la décision prise et sa date. Si \"garder la grille\" : rayer les étapes 54-56 de cette roadmap. Si \"construire le fil\" : préciser l'algorithme de tri souhaité (poids récence/popularité/suivi) pour cadrer l'Étape 54.", "validation": "Une décision écrite et datée existe dans ce document avant toute exécution des étapes suivantes de cette phase.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Trancher consciemment entre \"garder la grille catalogue comme interprétation MVP pragmatique\" et \"construire le fil vertical décrit dans le cahier des charges\", avant d'engager du temps de développement dessus.\n- **Fichiers impactés :** aucun (décision produit, pas de code)\n- **Instructions d'exécution :** Documenter dans ce fichier (note à ajouter sous cette étape) la décision prise et sa date. Si \"garder la grille\" : rayer les étapes 54-56 de cette roadmap. Si \"construire le fil\" : préciser l'algorithme de tri souhaité (poids récence/popularité/suivi) pour cadrer l'Étape 54.\n- **Critères de validation :** Une décision écrite et datée existe dans ce document avant toute exécution des étapes suivantes de cette phase.\n- **Statut :** [ ] Non commencé"}, {"id": 54, "title": "Adapter `GET /api/products` pour un tri \"feed\" (backend)", "objective": "Fournir un ordre de résultats mêlant récence, popularité et boutiques suivies, conformément à la décision de l'Étape 53.", "files": "`server-sanhia/routes/products.js`", "instructions": "Ajouter une valeur `sort=feed` calculant un score composite (ex. pondération récence décroissante + `likes._count` + bonus si `shopId` figure dans les `Follow` de l'utilisateur connecté, si authentifié). Conserver les valeurs de tri existantes (`recent`, `price-asc`, etc.) inchangées.", "validation": "`GET /api/products?sort=feed` avec un token d'utilisateur suivant au moins une boutique renvoie en priorité des produits de cette boutique par rapport à un appel identique sans authentification.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Fournir un ordre de résultats mêlant récence, popularité et boutiques suivies, conformément à la décision de l'Étape 53.\n- **Fichiers impactés :** `server-sanhia/routes/products.js`\n- **Instructions d'exécution :** Ajouter une valeur `sort=feed` calculant un score composite (ex. pondération récence décroissante + `likes._count` + bonus si `shopId` figure dans les `Follow` de l'utilisateur connecté, si authentifié). Conserver les valeurs de tri existantes (`recent`, `price-asc`, etc.) inchangées.\n- **Critères de validation :** `GET /api/products?sort=feed` avec un token d'utilisateur suivant au moins une boutique renvoie en priorité des produits de cette boutique par rapport à un appel identique sans authentification.\n- **Statut :** [ ] Non commencé"}, {"id": 55, "title": "Construire le composant Feed vertical plein écran (web)", "objective": "Remplacer (ou proposer en alternative à) la grille `Home.jsx` par un scroll séquentiel.", "files": "`sanhia-web/src/pages/Home.jsx`, nouveau composant `sanhia-web/src/components/feed/FeedCard.jsx`", "instructions": "Construire un composant affichant un produit à la fois en pleine largeur (image/vidéo, nom, prix, boutique, actions like/suivre/partager), avec défilement vertical (`scroll-snap-type: y mandatory` en CSS plutôt qu'une librairie tierce), alimenté par `GET /api/products?sort=feed`.", "validation": "La page d'accueil affiche un produit à la fois en plein écran, le défilement anime le passage au produit suivant, les actions like/suivre fonctionnent depuis la carte.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Remplacer (ou proposer en alternative à) la grille `Home.jsx` par un scroll séquentiel.\n- **Fichiers impactés :** `sanhia-web/src/pages/Home.jsx`, nouveau composant `sanhia-web/src/components/feed/FeedCard.jsx`\n- **Instructions d'exécution :** Construire un composant affichant un produit à la fois en pleine largeur (image/vidéo, nom, prix, boutique, actions like/suivre/partager), avec défilement vertical (`scroll-snap-type: y mandatory` en CSS plutôt qu'une librairie tierce), alimenté par `GET /api/products?sort=feed`.\n- **Critères de validation :** La page d'accueil affiche un produit à la fois en plein écran, le défilement anime le passage au produit suivant, les actions like/suivre fonctionnent depuis la carte.\n- **Statut :** [ ] Non commencé"}, {"id": 56, "title": "Construire le composant Feed vertical plein écran (mobile)", "objective": "Parité avec le web.", "files": "`sanhia-mobile/app/(buyer)/index.js`", "instructions": "Remplacer le `FlatList numColumns={2}` par un `FlatList` vertical `pagingEnabled` à un item par écran, alimenté par `sort=feed`, réutilisant les actions like/wishlist/contact déjà construites sur la fiche produit.", "validation": "L'onglet Accueil (mobile) affiche un produit à la fois en plein écran, avec un swipe vertical fluide vers le suivant.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Parité avec le web.\n- **Fichiers impactés :** `sanhia-mobile/app/(buyer)/index.js`\n- **Instructions d'exécution :** Remplacer le `FlatList numColumns={2}` par un `FlatList` vertical `pagingEnabled` à un item par écran, alimenté par `sort=feed`, réutilisant les actions like/wishlist/contact déjà construites sur la fiche produit.\n- **Critères de validation :** L'onglet Accueil (mobile) affiche un produit à la fois en plein écran, avec un swipe vertical fluide vers le suivant.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "O", "name": "Support vidéo produit (dépend de la Phase N si le fil doit lire de la vidéo)", "intro": "", "steps": [{"id": 57, "title": "Ajouter un champ vidéo au modèle `Product` (migration)", "objective": "Poser la fondation de données pour une courte vidéo par produit, en plus des photos.", "files": "`server-sanhia/prisma/schema.prisma`, nouvelle migration", "instructions": "Ajouter `videoUrl String?` au modèle `Product`. Lancer `npx prisma migrate dev --name add_product_video`.", "validation": "`npx prisma migrate status` ne montre aucune migration en attente.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Poser la fondation de données pour une courte vidéo par produit, en plus des photos.\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`, nouvelle migration\n- **Instructions d'exécution :** Ajouter `videoUrl String?` au modèle `Product`. Lancer `npx prisma migrate dev --name add_product_video`.\n- **Critères de validation :** `npx prisma migrate status` ne montre aucune migration en attente.\n- **Statut :** [ ] Non commencé"}, {"id": 58, "title": "Autoriser l'upload vidéo côté backend", "objective": "Permettre l'envoi d'une courte vidéo à la création de produit, avec des limites raisonnables.", "files": "`server-sanhia/routes/products.js`", "instructions": "Étendre `ALLOWED_MIME` avec `video/mp4`. Ajouter une limite de taille dédiée aux vidéos (ex. 15 Mo, plus haute que celle des images) via un champ multer distinct (`upload.fields([{ name: 'images', ... }, { name: 'video', maxCount: 1 }])`). Stocker l'URL résultante dans `videoUrl` (Étape 57).", "validation": "`POST /api/products` avec un fichier `video/mp4` de moins de 15 Mo aboutit à un produit dont `videoUrl` est renseigné ; un fichier vidéo de plus de 15 Mo est rejeté avec un message clair.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre l'envoi d'une courte vidéo à la création de produit, avec des limites raisonnables.\n- **Fichiers impactés :** `server-sanhia/routes/products.js`\n- **Instructions d'exécution :** Étendre `ALLOWED_MIME` avec `video/mp4`. Ajouter une limite de taille dédiée aux vidéos (ex. 15 Mo, plus haute que celle des images) via un champ multer distinct (`upload.fields([{ name: 'images', ... }, { name: 'video', maxCount: 1 }])`). Stocker l'URL résultante dans `videoUrl` (Étape 57).\n- **Critères de validation :** `POST /api/products` avec un fichier `video/mp4` de moins de 15 Mo aboutit à un produit dont `videoUrl` est renseigné ; un fichier vidéo de plus de 15 Mo est rejeté avec un message clair.\n- **Statut :** [ ] Non commencé"}, {"id": 59, "title": "Ajouter la sélection vidéo au formulaire d'ajout de produit (web)", "objective": "Rendre la fonctionnalité backend utilisable côté vendeur.", "files": "`sanhia-web/src/pages/Seller.jsx` (`AddProductTab`)", "instructions": "Ajouter un champ `<input type=\"file\" accept=\"video/mp4\">` optionnel, avec aperçu (`<video controls>`) avant soumission, ajouté au `FormData` existant sous la clé `video`.", "validation": "Publier un produit avec une courte vidéo depuis `/seller` aboutit à un produit dont la fiche affiche la vidéo (voir Étape 60).", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre la fonctionnalité backend utilisable côté vendeur.\n- **Fichiers impactés :** `sanhia-web/src/pages/Seller.jsx` (`AddProductTab`)\n- **Instructions d'exécution :** Ajouter un champ `<input type=\"file\" accept=\"video/mp4\">` optionnel, avec aperçu (`<video controls>`) avant soumission, ajouté au `FormData` existant sous la clé `video`.\n- **Critères de validation :** Publier un produit avec une courte vidéo depuis `/seller` aboutit à un produit dont la fiche affiche la vidéo (voir Étape 60).\n- **Statut :** [ ] Non commencé"}, {"id": 60, "title": "Ajouter la lecture vidéo sur la fiche produit (web)", "objective": "Afficher la vidéo si elle existe, en complément ou à la place de la galerie photo.", "files": "`sanhia-web/src/pages/Product.jsx`", "instructions": "Si `product.videoUrl` est renseigné, afficher un lecteur `<video controls poster={product.images?.[0]}>` en tête de la galerie, avant les vignettes photo.", "validation": "La fiche d'un produit avec vidéo affiche un lecteur fonctionnel ; un produit sans vidéo affiche la galerie photo classique, inchangée.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Afficher la vidéo si elle existe, en complément ou à la place de la galerie photo.\n- **Fichiers impactés :** `sanhia-web/src/pages/Product.jsx`\n- **Instructions d'exécution :** Si `product.videoUrl` est renseigné, afficher un lecteur `<video controls poster={product.images?.[0]}>` en tête de la galerie, avant les vignettes photo.\n- **Critères de validation :** La fiche d'un produit avec vidéo affiche un lecteur fonctionnel ; un produit sans vidéo affiche la galerie photo classique, inchangée.\n- **Statut :** [ ] Non commencé"}, {"id": 61, "title": "Ajouter la sélection vidéo au formulaire d'ajout de produit (mobile)", "objective": "Parité avec le web.", "files": "`sanhia-mobile/app/seller/add-product.js`", "instructions": "Étendre l'`ImagePicker` de l'Étape 34 avec `mediaTypes: ['images', 'videos']`, distinguer le type de l'asset sélectionné (`asset.type`) pour l'ajouter soit à `images`, soit au champ `video` du `FormData`.", "validation": "Sélectionner une courte vidéo depuis la galerie du téléphone et publier le produit aboutit à un produit dont la vidéo est lisible côté web.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Parité avec le web.\n- **Fichiers impactés :** `sanhia-mobile/app/seller/add-product.js`\n- **Instructions d'exécution :** Étendre l'`ImagePicker` de l'Étape 34 avec `mediaTypes: ['images', 'videos']`, distinguer le type de l'asset sélectionné (`asset.type`) pour l'ajouter soit à `images`, soit au champ `video` du `FormData`.\n- **Critères de validation :** Sélectionner une courte vidéo depuis la galerie du téléphone et publier le produit aboutit à un produit dont la vidéo est lisible côté web.\n- **Statut :** [ ] Non commencé"}, {"id": 62, "title": "Ajouter la lecture vidéo sur la fiche produit (mobile)", "objective": "Parité complète avec le web sur la vidéo produit.", "files": "`sanhia-mobile/app/product/[id].js`, nouvelle dépendance `expo-video`", "instructions": "Installer `expo-video` (`npx expo install expo-video`). Si `product.videoUrl` est renseigné, afficher un composant vidéo en tête d'écran (au lieu de l'`Image` statique), avec contrôles de lecture basiques.", "validation": "La fiche d'un produit avec vidéo (mobile) lit la vidéo avec contrôles ; nécessite un nouveau build EAS (nouveau module natif) pour être testable sur appareil.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Parité complète avec le web sur la vidéo produit.\n- **Fichiers impactés :** `sanhia-mobile/app/product/[id].js`, nouvelle dépendance `expo-video`\n- **Instructions d'exécution :** Installer `expo-video` (`npx expo install expo-video`). Si `product.videoUrl` est renseigné, afficher un composant vidéo en tête d'écran (au lieu de l'`Image` statique), avec contrôles de lecture basiques.\n- **Critères de validation :** La fiche d'un produit avec vidéo (mobile) lit la vidéo avec contrôles ; nécessite un nouveau build EAS (nouveau module natif) pour être testable sur appareil.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "P", "name": "Robustesse (basse priorité)", "intro": "", "steps": [{"id": 63, "title": "Ajouter un cache mémoire simple sur `GET /api/products` le plus consulté", "objective": "Réduire la charge Postgres sur la route la plus sollicitée, sans introduire de dépendance externe (Redis) tant que le volume ne le justifie pas.", "files": "`server-sanhia/routes/products.js`", "instructions": "Ajouter un cache en mémoire process (`Map` avec TTL de ~30s) clé sur les paramètres de requête sérialisés, pour les appels `GET /` sans authentification (feed public). Invalider explicitement l'entrée concernée à chaque création/modification de produit.", "validation": "Deux appels identiques à `GET /api/products` en moins de 30 secondes renvoient une réponse identique sans déclencher deux requêtes Postgres distinctes (vérifiable via les logs `log: ['query', ...]` déjà configurés en dev).", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Réduire la charge Postgres sur la route la plus sollicitée, sans introduire de dépendance externe (Redis) tant que le volume ne le justifie pas.\n- **Fichiers impactés :** `server-sanhia/routes/products.js`\n- **Instructions d'exécution :** Ajouter un cache en mémoire process (`Map` avec TTL de ~30s) clé sur les paramètres de requête sérialisés, pour les appels `GET /` sans authentification (feed public). Invalider explicitement l'entrée concernée à chaque création/modification de produit.\n- **Critères de validation :** Deux appels identiques à `GET /api/products` en moins de 30 secondes renvoient une réponse identique sans déclencher deux requêtes Postgres distinctes (vérifiable via les logs `log: ['query', ...]` déjà configurés en dev).\n- **Statut :** [ ] Non commencé"}, {"id": 64, "title": "Ajouter la persistance du cache et la détection de connectivité (mobile)", "objective": "Amorcer le \"mode dégradé\" recommandé par le cahier des charges pour les connexions instables.", "files": "`sanhia-mobile/app/_layout.js`, `sanhia-mobile/package.json`", "instructions": "Installer `@tanstack/query-async-storage-persister` et `@react-native-community/netinfo`. Configurer `persistQueryClient` avec `AsyncStorage` pour que les données déjà chargées restent consultables hors-ligne. Afficher un bandeau discret \"Connexion limitée\" quand `NetInfo` signale une perte de connexion.", "validation": "Couper la connexion réseau du téléphone après un premier chargement du Catalogue affiche toujours les produits déjà chargés (depuis le cache), avec le bandeau de connexion limitée visible.", "originalStatus": "[ ] Non commencé\n\n---", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Amorcer le \"mode dégradé\" recommandé par le cahier des charges pour les connexions instables.\n- **Fichiers impactés :** `sanhia-mobile/app/_layout.js`, `sanhia-mobile/package.json`\n- **Instructions d'exécution :** Installer `@tanstack/query-async-storage-persister` et `@react-native-community/netinfo`. Configurer `persistQueryClient` avec `AsyncStorage` pour que les données déjà chargées restent consultables hors-ligne. Afficher un bandeau discret \"Connexion limitée\" quand `NetInfo` signale une perte de connexion.\n- **Critères de validation :** Couper la connexion réseau du téléphone après un premier chargement du Catalogue affiche toujours les produits déjà chargés (depuis le cache), avec le bandeau de connexion limitée visible.\n- **Statut :** [ ] Non commencé\n\n---"}]}];
window.OUT_OF_SCOPE="## Hors périmètre de cette roadmap\n\nÉléments identifiés par l'audit mais volontairement exclus du découpage atomique ci-dessus :\n\n- **Nettoyage de l'historique git de `server-sanhia/node_modules`** (16 541 fichiers trackés) — nécessite une réécriture d'historique (`git filter-repo` ou équivalent) et un force-push coordonné avec toute l'équipe ayant un clone local ; opération à mener manuellement avec supervision humaine directe, pas comme une étape LLM autonome.\n- **Suite de tests automatisés** (backend, web, mobile) — transversal à toutes les phases ci-dessus plutôt qu'une étape isolée ; à cadrer comme un chantier séparé une fois qu'un socle de fonctionnalités stable existe, pour éviter d'écrire des tests contre du code encore en mouvement.\n- **Rotation effective du mot de passe Firebase admin** (Étape 1 ne fait que sécuriser le code, pas changer le mot de passe réel) — action à mener directement dans la console Firebase par un humain habilité.\n- **Compte admin (`aurumcorporate.d@gmail.com`) sans ligne Postgres correspondante** — découvert en testant l'Étape 7 : `requireAdmin` renvoie 403 même pour ce compte, car `prisma.user.findUnique` ne trouve rien pour son UID Firebase. Hypothèse de l'équipe : lié au passage à l'auth par numéro de téléphone (le compte admin, créé en email/mot de passe, ne passe jamais par le flux `POST /users` qui auto-provisionne la ligne Postgres au premier login OTP). À reprendre une fois la roadmap terminée — décider si l'admin doit aussi passer par l'auth téléphone, ou si `create-users.js` doit resynchroniser ce compte spécifiquement.";