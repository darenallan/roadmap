window.ROADMAP_DATA=[{"code": "A", "name": "Localisations pré-enregistrées", "intro": ">", "steps": [{"id": 1, "title": "Ajouter la route d'édition d'adresse (backend)", "objective": "Permettre à un client de modifier une adresse enregistrée, seule opération CRUD manquante côté backend.", "files": "`server-sanhia/routes/users.routes.js`", "instructions": "Ajouter `PATCH /api/users/me/addresses/:id`, protégée par `verifyToken`. Vérifier que l'adresse appartient bien à `req.user.uid` (404 sinon, même pattern que `DELETE /api/users/me/addresses/:id`). Accepter en body un sous-ensemble de `{ name, phone, city, desc, tag, lat, lng, isDefault }` (mise à jour partielle). Si `isDefault: true` est envoyé, repasser les autres adresses du même utilisateur à `isDefault: false` dans la même transaction (cohérence avec le comportement déjà attendu par le front sur `POST`).", "validation": "`PATCH /api/users/me/addresses/:id` sur une adresse existante renvoie 200 avec les champs modifiés ; la même requête sur l'adresse d'un autre utilisateur renvoie 404.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre à un client de modifier une adresse enregistrée, seule opération CRUD manquante côté backend.\n- **Fichiers impactés :** `server-sanhia/routes/users.routes.js`\n- **Instructions d'exécution :** Ajouter `PATCH /api/users/me/addresses/:id`, protégée par `verifyToken`. Vérifier que l'adresse appartient bien à `req.user.uid` (404 sinon, même pattern que `DELETE /api/users/me/addresses/:id`). Accepter en body un sous-ensemble de `{ name, phone, city, desc, tag, lat, lng, isDefault }` (mise à jour partielle). Si `isDefault: true` est envoyé, repasser les autres adresses du même utilisateur à `isDefault: false` dans la même transaction (cohérence avec le comportement déjà attendu par le front sur `POST`).\n- **Critères de validation :** `PATCH /api/users/me/addresses/:id` sur une adresse existante renvoie 200 avec les champs modifiés ; la même requête sur l'adresse d'un autre utilisateur renvoie 404.\n- **Statut :** [ ] Non commencé"}, {"id": 2, "title": "Ajouter l'édition dans l'onglet Adresses (web)", "objective": "Rendre la route d'édition utilisable depuis le profil web.", "files": "`sanhia-web/src/pages/Profile.jsx` (composant `TabAdresses`)", "instructions": "Ajouter un bouton \"Modifier\" sur chaque carte d'adresse, ouvrant le même formulaire que \"Ajouter\" pré-rempli avec les valeurs existantes, soumettant vers `PATCH /api/users/me/addresses/:id` au lieu de `POST`. Réutiliser l'état de formulaire déjà existant pour l'ajout plutôt que d'en dupliquer un.", "validation": "Modifier le libellé d'une adresse depuis `/profile` → onglet Adresses met à jour la carte affichée sans rechargement de page, et persiste après rafraîchissement.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Rendre la route d'édition utilisable depuis le profil web.\n- **Fichiers impactés :** `sanhia-web/src/pages/Profile.jsx` (composant `TabAdresses`)\n- **Instructions d'exécution :** Ajouter un bouton \"Modifier\" sur chaque carte d'adresse, ouvrant le même formulaire que \"Ajouter\" pré-rempli avec les valeurs existantes, soumettant vers `PATCH /api/users/me/addresses/:id` au lieu de `POST`. Réutiliser l'état de formulaire déjà existant pour l'ajout plutôt que d'en dupliquer un.\n- **Critères de validation :** Modifier le libellé d'une adresse depuis `/profile` → onglet Adresses met à jour la carte affichée sans rechargement de page, et persiste après rafraîchissement.\n- **Statut :** [ ] Non commencé"}, {"id": 3, "title": "Créer l'écran \"Mes adresses\" côté mobile", "objective": "Combler l'absence totale d'UI mobile pour le carnet d'adresses (liste, ajout, édition, suppression).", "files": "`sanhia-mobile/app/(buyer)/addresses.js` (nouveau), `sanhia-mobile/app/(buyer)/profile.js`", "instructions": "Nouvel écran listant `GET /api/users/me/addresses`, avec formulaire d'ajout/édition (nom, téléphone, ville, description, tag, `isDefault`) réutilisant les composants existants (`Button`, `Text`, `EmptyState` si liste vide). Pas de sélection de point sur carte (aucune lib de carte dans les dépendances mobiles actuelles) — `lat`/`lng` restent optionnels et saisis nulle part ici, cohérent avec le fait que `Address.lat/lng` sont déjà nullable côté schéma. Ajouter un lien \"Mes adresses\" dans `profile.js`, au même emplacement que les autres liens de compte existants.", "validation": "Depuis l'app mobile, ajouter une adresse, la modifier, puis la supprimer aboutit aux 3 opérations reflétées immédiatement dans la liste, et visibles côté web (`/profile` → Adresses) après rafraîchissement.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Combler l'absence totale d'UI mobile pour le carnet d'adresses (liste, ajout, édition, suppression).\n- **Fichiers impactés :** `sanhia-mobile/app/(buyer)/addresses.js` (nouveau), `sanhia-mobile/app/(buyer)/profile.js`\n- **Instructions d'exécution :** Nouvel écran listant `GET /api/users/me/addresses`, avec formulaire d'ajout/édition (nom, téléphone, ville, description, tag, `isDefault`) réutilisant les composants existants (`Button`, `Text`, `EmptyState` si liste vide). Pas de sélection de point sur carte (aucune lib de carte dans les dépendances mobiles actuelles) — `lat`/`lng` restent optionnels et saisis nulle part ici, cohérent avec le fait que `Address.lat/lng` sont déjà nullable côté schéma. Ajouter un lien \"Mes adresses\" dans `profile.js`, au même emplacement que les autres liens de compte existants.\n- **Critères de validation :** Depuis l'app mobile, ajouter une adresse, la modifier, puis la supprimer aboutit aux 3 opérations reflétées immédiatement dans la liste, et visibles côté web (`/profile` → Adresses) après rafraîchissement.\n- **Statut :** [ ] Non commencé"}, {"id": 4, "title": "Ajouter les champs de snapshot d'adresse au modèle `Order` et `lat/lng` à `Livraison` (migration Prisma)", "objective": "Donner à la commande un endroit où stocker l'adresse choisie au moment du checkout, indépendamment de toute modification/suppression ultérieure de l'adresse enregistrée (snapshot, même logique que `OrderItem.price`).", "files": "`server-sanhia/prisma/schema.prisma`", "instructions": "Sur `model Order`, ajouter `addressLabel String?`, `addressText String?`, `addressLat Float?`, `addressLng Float?`. Sur `model Livraison`, ajouter `lat Float?`, `lng Float?` (pour transmettre une position précise au livreur, en plus du `zone` textuel déjà existant). Appliquer avec `npx prisma db push` (pas `migrate dev` — ce projet n'a pas d'historique Prisma Migrate, voir précédent de l'Étape 19/29/41/47/57 de ROADMAP.md).", "validation": "`npx prisma db push` s'exécute sans erreur ; `npx prisma studio` (ou une requête directe) montre les nouvelles colonnes sur `Order` et `Livraison`, toutes nullables, sans perte de données existantes.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Donner à la commande un endroit où stocker l'adresse choisie au moment du checkout, indépendamment de toute modification/suppression ultérieure de l'adresse enregistrée (snapshot, même logique que `OrderItem.price`).\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`\n- **Instructions d'exécution :** Sur `model Order`, ajouter `addressLabel String?`, `addressText String?`, `addressLat Float?`, `addressLng Float?`. Sur `model Livraison`, ajouter `lat Float?`, `lng Float?` (pour transmettre une position précise au livreur, en plus du `zone` textuel déjà existant). Appliquer avec `npx prisma db push` (pas `migrate dev` — ce projet n'a pas d'historique Prisma Migrate, voir précédent de l'Étape 19/29/41/47/57 de ROADMAP.md).\n- **Critères de validation :** `npx prisma db push` s'exécute sans erreur ; `npx prisma studio` (ou une requête directe) montre les nouvelles colonnes sur `Order` et `Livraison`, toutes nullables, sans perte de données existantes.\n- **Statut :** [ ] Non commencé"}, {"id": 5, "title": "Accepter une adresse (enregistrée ou ponctuelle) à la création de commande (backend)", "objective": "Permettre au client de transmettre une adresse de livraison réelle à `POST /api/orders`, avec repli sur saisie libre si aucune adresse n'est enregistrée (US-V1.5-04 : la commande sans adresse enregistrée doit rester possible).", "files": "`server-sanhia/routes/orders.js`", "instructions": "Étendre `createOrderSchema` (zod) pour accepter en option soit `addressId` (string), soit un objet `address: { label, text, lat, lng }` en saisie libre — les deux optionnels, ni l'un ni l'autre requis. Si `addressId` fourni : charger l'adresse via `prisma.address.findUnique`, vérifier `userId === req.user.uid` (400 sinon), puis copier ses champs dans `addressLabel/addressText/addressLat/addressLng` de la commande créée. Si `address` (saisie libre) fourni : copier directement ces valeurs. Si ni l'un ni l'autre : laisser les 4 champs à `null` (comportement actuel inchangé).", "validation": "Créer une commande avec `addressId` valide stocke les 4 champs snapshot corrects sur l'`Order` créé (vérifiable via `GET /api/orders/:id`) ; créer une commande sans aucun champ d'adresse continue de fonctionner comme avant (aucune régression) ; un `addressId` appartenant à un autre utilisateur est rejeté en 400.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre au client de transmettre une adresse de livraison réelle à `POST /api/orders`, avec repli sur saisie libre si aucune adresse n'est enregistrée (US-V1.5-04 : la commande sans adresse enregistrée doit rester possible).\n- **Fichiers impactés :** `server-sanhia/routes/orders.js`\n- **Instructions d'exécution :** Étendre `createOrderSchema` (zod) pour accepter en option soit `addressId` (string), soit un objet `address: { label, text, lat, lng }` en saisie libre — les deux optionnels, ni l'un ni l'autre requis. Si `addressId` fourni : charger l'adresse via `prisma.address.findUnique`, vérifier `userId === req.user.uid` (400 sinon), puis copier ses champs dans `addressLabel/addressText/addressLat/addressLng` de la commande créée. Si `address` (saisie libre) fourni : copier directement ces valeurs. Si ni l'un ni l'autre : laisser les 4 champs à `null` (comportement actuel inchangé).\n- **Critères de validation :** Créer une commande avec `addressId` valide stocke les 4 champs snapshot corrects sur l'`Order` créé (vérifiable via `GET /api/orders/:id`) ; créer une commande sans aucun champ d'adresse continue de fonctionner comme avant (aucune régression) ; un `addressId` appartenant à un autre utilisateur est rejeté en 400.\n- **Statut :** [ ] Non commencé"}, {"id": 6, "title": "Utiliser l'adresse de la commande pour la zone/position de la Livraison (backend)", "objective": "Remplacer le repli actuel (ville de la boutique) par la vraie adresse client quand elle est disponible, sans casser le comportement existant quand elle ne l'est pas.", "files": "`server-sanhia/routes/admin.routes.js` (2 points de création de `Livraison` : approbation commande directe et validation de paiement)", "instructions": "Aux deux endroits où `prisma.livraison.create` est appelé, récupérer `order.addressLabel`, `order.addressLat`, `order.addressLng` (déjà chargeable via le `orderId`). Utiliser `order.addressLabel ?? <ville boutique actuelle>` pour `zone` (repli identique à l'existant si aucune adresse n'a été fournie), et passer `lat: order.addressLat, lng: order.addressLng` sur la `Livraison` créée.", "validation": "Approuver une commande créée avec une adresse (Étape 5) produit une `Livraison` avec `zone` = libellé de l'adresse et `lat`/`lng` renseignés ; approuver une commande sans adresse produit une `Livraison` identique à avant (zone = ville boutique, lat/lng null) — aucune régression sur le flux existant.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Remplacer le repli actuel (ville de la boutique) par la vraie adresse client quand elle est disponible, sans casser le comportement existant quand elle ne l'est pas.\n- **Fichiers impactés :** `server-sanhia/routes/admin.routes.js` (2 points de création de `Livraison` : approbation commande directe et validation de paiement)\n- **Instructions d'exécution :** Aux deux endroits où `prisma.livraison.create` est appelé, récupérer `order.addressLabel`, `order.addressLat`, `order.addressLng` (déjà chargeable via le `orderId`). Utiliser `order.addressLabel ?? <ville boutique actuelle>` pour `zone` (repli identique à l'existant si aucune adresse n'a été fournie), et passer `lat: order.addressLat, lng: order.addressLng` sur la `Livraison` créée.\n- **Critères de validation :** Approuver une commande créée avec une adresse (Étape 5) produit une `Livraison` avec `zone` = libellé de l'adresse et `lat`/`lng` renseignés ; approuver une commande sans adresse produit une `Livraison` identique à avant (zone = ville boutique, lat/lng null) — aucune régression sur le flux existant.\n- **Statut :** [ ] Non commencé"}, {"id": 7, "title": "Brancher la sélection d'adresse réelle au checkout (web)", "objective": "Remplacer le `<select>` cosmétique à 4 options par une vraie sélection parmi les adresses enregistrées du client, avec repli en saisie libre.", "files": "`sanhia-web/src/pages/Cart.jsx`", "instructions": "Charger `GET /api/users/me/addresses` à l'ouverture de la page (React Query ou `useEffect`+`useState`, selon le pattern déjà dominant dans ce fichier). Remplacer les 4 `<option>` codées en dur par la liste réelle des adresses du client (libellé = `tag`/`name` + `city`), plus une option \"Nouvelle adresse\" ouvrant 2-3 champs de saisie libre (texte + ville) si le client n'a rien d'enregistré ou veut saisir autre chose. Dans `handleCheckout`, inclure `addressId` (si une adresse enregistrée est sélectionnée) ou `address: { label, text }` (si saisie libre) dans le `payload` envoyé à `POST /api/orders`. Le frais d'affichage (`SHIPPING_FEE`) peut rester basé sur la simple présence d'une adresse sélectionnée, inchangé.", "validation": "Passer une commande en sélectionnant une adresse enregistrée aboutit à un `Order` dont `addressLabel`/`addressText` correspondent à cette adresse (vérifiable côté backend) ; passer une commande sans adresse enregistrée mais avec saisie libre aboutit également à un `Order` avec adresse renseignée.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Remplacer le `<select>` cosmétique à 4 options par une vraie sélection parmi les adresses enregistrées du client, avec repli en saisie libre.\n- **Fichiers impactés :** `sanhia-web/src/pages/Cart.jsx`\n- **Instructions d'exécution :** Charger `GET /api/users/me/addresses` à l'ouverture de la page (React Query ou `useEffect`+`useState`, selon le pattern déjà dominant dans ce fichier). Remplacer les 4 `<option>` codées en dur par la liste réelle des adresses du client (libellé = `tag`/`name` + `city`), plus une option \"Nouvelle adresse\" ouvrant 2-3 champs de saisie libre (texte + ville) si le client n'a rien d'enregistré ou veut saisir autre chose. Dans `handleCheckout`, inclure `addressId` (si une adresse enregistrée est sélectionnée) ou `address: { label, text }` (si saisie libre) dans le `payload` envoyé à `POST /api/orders`. Le frais d'affichage (`SHIPPING_FEE`) peut rester basé sur la simple présence d'une adresse sélectionnée, inchangé.\n- **Critères de validation :** Passer une commande en sélectionnant une adresse enregistrée aboutit à un `Order` dont `addressLabel`/`addressText` correspondent à cette adresse (vérifiable côté backend) ; passer une commande sans adresse enregistrée mais avec saisie libre aboutit également à un `Order` avec adresse renseignée.\n- **Statut :** [ ] Non commencé"}, {"id": 8, "title": "Ajouter la sélection d'adresse au checkout (mobile)", "objective": "Porter la même intégration checkout sur mobile, qui n'a aujourd'hui aucune notion d'adresse au panier.", "files": "`sanhia-mobile/app/(buyer)/cart.js`", "instructions": "Avant le bouton \"Passer la commande\", charger `GET /api/users/me/addresses` et afficher un sélecteur simple (liste de boutons/rows, une adresse cochée à la fois — pas besoin de `<select>` natif, suivre le pattern déjà utilisé pour d'autres choix courts dans l'app). Si aucune adresse enregistrée, permettre de continuer sans (repli identique au comportement actuel, aucune adresse envoyée). Inclure `addressId` dans le `payload` de `handleCheckout` si une adresse est sélectionnée.", "validation": "Depuis l'app mobile, passer une commande avec une adresse enregistrée sélectionnée aboutit à un `Order` dont les champs d'adresse sont renseignés (vérifiable côté backend) ; passer une commande sans sélection continue de fonctionner comme avant.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Porter la même intégration checkout sur mobile, qui n'a aujourd'hui aucune notion d'adresse au panier.\n- **Fichiers impactés :** `sanhia-mobile/app/(buyer)/cart.js`\n- **Instructions d'exécution :** Avant le bouton \"Passer la commande\", charger `GET /api/users/me/addresses` et afficher un sélecteur simple (liste de boutons/rows, une adresse cochée à la fois — pas besoin de `<select>` natif, suivre le pattern déjà utilisé pour d'autres choix courts dans l'app). Si aucune adresse enregistrée, permettre de continuer sans (repli identique au comportement actuel, aucune adresse envoyée). Inclure `addressId` dans le `payload` de `handleCheckout` si une adresse est sélectionnée.\n- **Critères de validation :** Depuis l'app mobile, passer une commande avec une adresse enregistrée sélectionnée aboutit à un `Order` dont les champs d'adresse sont renseignés (vérifiable côté backend) ; passer une commande sans sélection continue de fonctionner comme avant.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "B", "name": "Stories boutiques", "intro": ">", "steps": [{"id": 9, "title": "Ajouter le modèle `Story` (migration Prisma)", "objective": "Créer la table de stockage des stories, liée aux boutiques.", "files": "`server-sanhia/prisma/schema.prisma`", "instructions": "Ajouter `model Story { id String @id @default(cuid()); shopId String; shop Shop @relation(fields: [shopId], references: [id]); mediaUrl String; type String; createdAt DateTime @default(now()) }` (`type` = `\"photo\"` ou `\"video\"`). Ajouter la relation inverse `stories Story[]` sur `model Shop`. Appliquer avec `npx prisma db push`.", "validation": "`npx prisma db push` s'exécute sans erreur ; la table `Story` est visible dans `npx prisma studio`.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Créer la table de stockage des stories, liée aux boutiques.\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`\n- **Instructions d'exécution :** Ajouter `model Story { id String @id @default(cuid()); shopId String; shop Shop @relation(fields: [shopId], references: [id]); mediaUrl String; type String; createdAt DateTime @default(now()) }` (`type` = `\"photo\"` ou `\"video\"`). Ajouter la relation inverse `stories Story[]` sur `model Shop`. Appliquer avec `npx prisma db push`.\n- **Critères de validation :** `npx prisma db push` s'exécute sans erreur ; la table `Story` est visible dans `npx prisma studio`.\n- **Statut :** [ ] Non commencé"}, {"id": 10, "title": "Ajouter la route de publication de story (backend)", "objective": "Permettre à une boutique de publier une story (photo ou vidéo courte).", "files": "`server-sanhia/routes/shops.routes.js`", "instructions": "Ajouter `POST /api/shops/:id/stories`, protégée par `verifyToken` + vérification que `req.user.uid` possède bien la boutique `:id` (même pattern IDOR déjà en place sur `PATCH /api/shops/:id`). Multer en mémoire, un seul champ `media` (image ou `video/mp4`, réutiliser `ALLOWED_IMAGE_MIME`/la logique de `fileFilter` de `products.js`). Upload via `uploadToStorage` existant. Déterminer `type` à partir du mimetype reçu (`image/*` → `\"photo\"`, `video/mp4` → `\"video\"`). Créer la `Story`.", "validation": "`POST /api/shops/:id/stories` avec une image en multipart renvoie 201 avec une story dont `mediaUrl` pointe vers un fichier réellement accessible (R2) ; un utilisateur qui ne possède pas la boutique reçoit 403.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre à une boutique de publier une story (photo ou vidéo courte).\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js`\n- **Instructions d'exécution :** Ajouter `POST /api/shops/:id/stories`, protégée par `verifyToken` + vérification que `req.user.uid` possède bien la boutique `:id` (même pattern IDOR déjà en place sur `PATCH /api/shops/:id`). Multer en mémoire, un seul champ `media` (image ou `video/mp4`, réutiliser `ALLOWED_IMAGE_MIME`/la logique de `fileFilter` de `products.js`). Upload via `uploadToStorage` existant. Déterminer `type` à partir du mimetype reçu (`image/*` → `\"photo\"`, `video/mp4` → `\"video\"`). Créer la `Story`.\n- **Critères de validation :** `POST /api/shops/:id/stories` avec une image en multipart renvoie 201 avec une story dont `mediaUrl` pointe vers un fichier réellement accessible (R2) ; un utilisateur qui ne possède pas la boutique reçoit 403.\n- **Statut :** [ ] Non commencé"}, {"id": 11, "title": "Ajouter les routes de lecture des stories (backend)", "objective": "Exposer la liste des stories actives d'une boutique (gestion boutique) et le fil des stories des boutiques suivies (client), avec expiration filtrée à la lecture (pas de tâche planifiée, conforme au cahier §5.3).", "files": "`server-sanhia/routes/shops.routes.js`", "instructions": "Ajouter `GET /api/shops/:id/stories` (les stories de la boutique avec `createdAt > now() - 24h`, triées par date décroissante — utile pour l'écran de gestion boutique). Ajouter `GET /api/stories/feed` (protégée par `verifyToken`) : stories des boutiques suivies par `req.user.uid` (jointure via `Follow`), filtrées sur les dernières 24h, groupées par boutique (une entrée par boutique avec sa story la plus récente + le compte total actif).", "validation": "Publier une story pour une boutique suivie par le compte de test la fait apparaître dans `GET /api/stories/feed` pour ce compte ; elle n'apparaît pas dans le feed d'un compte qui ne suit pas cette boutique.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Exposer la liste des stories actives d'une boutique (gestion boutique) et le fil des stories des boutiques suivies (client), avec expiration filtrée à la lecture (pas de tâche planifiée, conforme au cahier §5.3).\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js`\n- **Instructions d'exécution :** Ajouter `GET /api/shops/:id/stories` (les stories de la boutique avec `createdAt > now() - 24h`, triées par date décroissante — utile pour l'écran de gestion boutique). Ajouter `GET /api/stories/feed` (protégée par `verifyToken`) : stories des boutiques suivies par `req.user.uid` (jointure via `Follow`), filtrées sur les dernières 24h, groupées par boutique (une entrée par boutique avec sa story la plus récente + le compte total actif).\n- **Critères de validation :** Publier une story pour une boutique suivie par le compte de test la fait apparaître dans `GET /api/stories/feed` pour ce compte ; elle n'apparaît pas dans le feed d'un compte qui ne suit pas cette boutique.\n- **Statut :** [ ] Non commencé"}, {"id": 12, "title": "Créer l'écran de publication de story (web, dashboard vendeur)", "objective": "Donner aux boutiques un moyen de publier une story depuis le web.", "files": "`sanhia-web/src/pages/Seller.jsx`", "instructions": "Ajouter un nouvel onglet ou une section \"Story\" dans le dashboard vendeur, avec un input file (image ou vidéo courte) + aperçu avant envoi, soumettant vers `POST /api/shops/:id/stories` en `multipart/form-data` (attention au bug déjà corrigé ailleurs dans ce fichier : ne pas fixer manuellement le `Content-Type`, laisser le navigateur générer le boundary). Afficher un message de succès et la liste des stories actives de la boutique (via `GET /api/shops/:id/stories`) sous le formulaire.", "validation": "Publier une story depuis `/seller` aboutit à une entrée visible dans la liste sous le formulaire, sans erreur réseau.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Donner aux boutiques un moyen de publier une story depuis le web.\n- **Fichiers impactés :** `sanhia-web/src/pages/Seller.jsx`\n- **Instructions d'exécution :** Ajouter un nouvel onglet ou une section \"Story\" dans le dashboard vendeur, avec un input file (image ou vidéo courte) + aperçu avant envoi, soumettant vers `POST /api/shops/:id/stories` en `multipart/form-data` (attention au bug déjà corrigé ailleurs dans ce fichier : ne pas fixer manuellement le `Content-Type`, laisser le navigateur générer le boundary). Afficher un message de succès et la liste des stories actives de la boutique (via `GET /api/shops/:id/stories`) sous le formulaire.\n- **Critères de validation :** Publier une story depuis `/seller` aboutit à une entrée visible dans la liste sous le formulaire, sans erreur réseau.\n- **Statut :** [ ] Non commencé"}, {"id": 13, "title": "Afficher le bandeau de stories et le visionnage (web, fil de découverte)", "objective": "Afficher les stories actives des boutiques suivies en tête du fil de découverte, avec un visionnage plein écran.", "files": "`sanhia-web/src/pages/Home.jsx`", "instructions": "En tête de page (avant le contenu existant), ajouter un bandeau horizontal scrollable des boutiques suivies ayant une story active (`GET /api/stories/feed`), chaque avatar affichant un anneau distinctif. Rester discret conformément au point d'attention du cahier (§6) — pas de composant envahissant. Au clic sur un avatar, ouvrir une superposition plein écran affichant la/les story(ies) de cette boutique (image ou `<video autoPlay>`), avec navigation \"suivant\" (tap/flèche) passant à la story suivante puis à la boutique suivante, et fermeture (croix ou clic hors zone). Ne rien afficher si le client ne suit aucune boutique avec story active (bandeau absent, pas de zone vide).", "validation": "Un compte suivant une boutique avec story active voit le bandeau sur `/` ; cliquer dessus ouvre le visionnage plein écran de la story publiée à l'Étape 12 ; un compte ne suivant personne (ou dont les boutiques suivies n'ont pas de story active) ne voit pas le bandeau.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Afficher les stories actives des boutiques suivies en tête du fil de découverte, avec un visionnage plein écran.\n- **Fichiers impactés :** `sanhia-web/src/pages/Home.jsx`\n- **Instructions d'exécution :** En tête de page (avant le contenu existant), ajouter un bandeau horizontal scrollable des boutiques suivies ayant une story active (`GET /api/stories/feed`), chaque avatar affichant un anneau distinctif. Rester discret conformément au point d'attention du cahier (§6) — pas de composant envahissant. Au clic sur un avatar, ouvrir une superposition plein écran affichant la/les story(ies) de cette boutique (image ou `<video autoPlay>`), avec navigation \"suivant\" (tap/flèche) passant à la story suivante puis à la boutique suivante, et fermeture (croix ou clic hors zone). Ne rien afficher si le client ne suit aucune boutique avec story active (bandeau absent, pas de zone vide).\n- **Critères de validation :** Un compte suivant une boutique avec story active voit le bandeau sur `/` ; cliquer dessus ouvre le visionnage plein écran de la story publiée à l'Étape 12 ; un compte ne suivant personne (ou dont les boutiques suivies n'ont pas de story active) ne voit pas le bandeau.\n- **Statut :** [ ] Non commencé"}, {"id": 14, "title": "Créer l'écran de publication de story (mobile, côté vendeur)", "objective": "Porter la publication de story sur mobile.", "files": "`sanhia-mobile/app/(seller)/story.js` (nouveau), `sanhia-mobile/app/(seller)/index.js` ou `settings.js` (point d'entrée)", "instructions": "Nouvel écran réutilisant `expo-image-picker` (déjà installé et utilisé pour l'ajout de produit) pour sélectionner une image ou une courte vidéo depuis la galerie, aperçu avant envoi, soumission multipart vers `POST /api/shops/:id/stories` (même pattern que `app/seller/add-product.js`). Ajouter un point d'entrée (\"Publier une story\") depuis le tableau de bord vendeur mobile existant.", "validation": "Publier une story depuis l'app mobile vendeur aboutit à une entrée visible côté web dans la liste de gestion (Étape 12) sous quelques secondes.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Porter la publication de story sur mobile.\n- **Fichiers impactés :** `sanhia-mobile/app/(seller)/story.js` (nouveau), `sanhia-mobile/app/(seller)/index.js` ou `settings.js` (point d'entrée)\n- **Instructions d'exécution :** Nouvel écran réutilisant `expo-image-picker` (déjà installé et utilisé pour l'ajout de produit) pour sélectionner une image ou une courte vidéo depuis la galerie, aperçu avant envoi, soumission multipart vers `POST /api/shops/:id/stories` (même pattern que `app/seller/add-product.js`). Ajouter un point d'entrée (\"Publier une story\") depuis le tableau de bord vendeur mobile existant.\n- **Critères de validation :** Publier une story depuis l'app mobile vendeur aboutit à une entrée visible côté web dans la liste de gestion (Étape 12) sous quelques secondes.\n- **Statut :** [ ] Non commencé"}, {"id": 15, "title": "Afficher le bandeau de stories et le visionnage (mobile, fil de découverte)", "objective": "Porter le bandeau et le visionnage plein écran sur mobile.", "files": "`sanhia-mobile/app/(buyer)/index.js`", "instructions": "Même logique que l'Étape 13 : bandeau horizontal (`ScrollView horizontal` ou `FlatList horizontal`) en tête de `index.js`, alimenté par `GET /api/stories/feed`. Le visionnage plein écran est un nouvel écran (`app/story/[shopId].js`) avec `Image`/`useVideoPlayer`+`VideoView` (pattern déjà utilisé dans `app/product/[id].js`) et navigation tap gauche/droite entre stories.", "validation": "Depuis l'app mobile, le bandeau apparaît pour un compte suivant une boutique avec story active, et le tap ouvre le visionnage plein écran correspondant.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Porter le bandeau et le visionnage plein écran sur mobile.\n- **Fichiers impactés :** `sanhia-mobile/app/(buyer)/index.js`\n- **Instructions d'exécution :** Même logique que l'Étape 13 : bandeau horizontal (`ScrollView horizontal` ou `FlatList horizontal`) en tête de `index.js`, alimenté par `GET /api/stories/feed`. Le visionnage plein écran est un nouvel écran (`app/story/[shopId].js`) avec `Image`/`useVideoPlayer`+`VideoView` (pattern déjà utilisé dans `app/product/[id].js`) et navigation tap gauche/droite entre stories.\n- **Critères de validation :** Depuis l'app mobile, le bandeau apparaît pour un compte suivant une boutique avec story active, et le tap ouvre le visionnage plein écran correspondant.\n- **Statut :** [ ] Non commencé\n\n---"}]}, {"code": "C", "name": "Feed vidéo immersif", "intro": ">", "steps": [{"id": 16, "title": "Ajouter les modèles `Video` et `VideoProductTag` (migration Prisma)", "objective": "Créer les tables de stockage des vidéos du feed et de leurs tags produit par intervalle.", "files": "`server-sanhia/prisma/schema.prisma`", "instructions": "Ajouter `model Video { id String @id @default(cuid()); shopId String; shop Shop @relation(fields: [shopId], references: [id]); mediaUrl String; description String?; status String @default(\"pending\"); createdAt DateTime @default(now()); tags VideoProductTag[] }`. Ajouter `model VideoProductTag { id String @id @default(cuid()); videoId String; video Video @relation(fields: [videoId], references: [id]); productId String; product Product @relation(fields: [productId], references: [id]); startTime Float; endTime Float }`. Ajouter les relations inverses `videos Video[]` sur `model Shop` et `videoTags VideoProductTag[]` sur `model Product`. Appliquer avec `npx prisma db push`.", "validation": "`npx prisma db push` s'exécute sans erreur ; les tables `Video` et `VideoProductTag` sont visibles dans `npx prisma studio`.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Créer les tables de stockage des vidéos du feed et de leurs tags produit par intervalle.\n- **Fichiers impactés :** `server-sanhia/prisma/schema.prisma`\n- **Instructions d'exécution :** Ajouter `model Video { id String @id @default(cuid()); shopId String; shop Shop @relation(fields: [shopId], references: [id]); mediaUrl String; description String?; status String @default(\"pending\"); createdAt DateTime @default(now()); tags VideoProductTag[] }`. Ajouter `model VideoProductTag { id String @id @default(cuid()); videoId String; video Video @relation(fields: [videoId], references: [id]); productId String; product Product @relation(fields: [productId], references: [id]); startTime Float; endTime Float }`. Ajouter les relations inverses `videos Video[]` sur `model Shop` et `videoTags VideoProductTag[]` sur `model Product`. Appliquer avec `npx prisma db push`.\n- **Critères de validation :** `npx prisma db push` s'exécute sans erreur ; les tables `Video` et `VideoProductTag` sont visibles dans `npx prisma studio`.\n- **Statut :** [ ] Non commencé"}, {"id": 17, "title": "Ajouter la route d'upload vidéo (backend)", "objective": "Permettre à une boutique d'importer une vidéo pour le feed, en attente de modération.", "files": "`server-sanhia/routes/shops.routes.js`", "instructions": "Ajouter `POST /api/shops/:id/videos`, protégée par `verifyToken` + vérification de propriété (même pattern que l'Étape 10). Réutiliser la config multer de `products.js` (limite de taille, `fileFilter` sur `video/mp4`) — soit en important cette config, soit en la dupliquant localement si l'import croise des routes non liées (juger sur place selon la structure du fichier). Upload via `uploadToStorage` (passthrough vidéo tel quel, sans compression — voir constat n°4 en tête de document). Créer la `Video` avec `status: \"pending\"`.", "validation": "`POST /api/shops/:id/videos` avec un fichier `video/mp4` valide renvoie 201 avec `status: \"pending\"` et un `mediaUrl` accessible ; un fichier d'un autre type est rejeté.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre à une boutique d'importer une vidéo pour le feed, en attente de modération.\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js`\n- **Instructions d'exécution :** Ajouter `POST /api/shops/:id/videos`, protégée par `verifyToken` + vérification de propriété (même pattern que l'Étape 10). Réutiliser la config multer de `products.js` (limite de taille, `fileFilter` sur `video/mp4`) — soit en important cette config, soit en la dupliquant localement si l'import croise des routes non liées (juger sur place selon la structure du fichier). Upload via `uploadToStorage` (passthrough vidéo tel quel, sans compression — voir constat n°4 en tête de document). Créer la `Video` avec `status: \"pending\"`.\n- **Critères de validation :** `POST /api/shops/:id/videos` avec un fichier `video/mp4` valide renvoie 201 avec `status: \"pending\"` et un `mediaUrl` accessible ; un fichier d'un autre type est rejeté.\n- **Statut :** [ ] Non commencé"}, {"id": 18, "title": "Ajouter les routes de tagging produit sur vidéo (backend)", "objective": "Permettre à la boutique d'associer un produit de son catalogue à un intervalle de temps précis sur sa vidéo.", "files": "`server-sanhia/routes/shops.routes.js`", "instructions": "Ajouter `POST /api/videos/:id/tags` (body : `{ productId, startTime, endTime }`), vérifiant que la vidéo appartient à une boutique possédée par `req.user.uid` ET que le produit appartient à la même boutique (éviter le tag d'un produit d'une autre boutique). Ajouter `DELETE /api/videos/:id/tags/:tagId` (même vérification de propriété). Ajouter `GET /api/shops/:id/videos` (liste des vidéos de la boutique, tous statuts confondus, avec leurs tags — pour l'écran de gestion boutique).", "validation": "Taguer un produit de la boutique sur une vidéo qu'elle possède renvoie 201 ; taguer un produit d'une autre boutique est rejeté en 400 ; `DELETE` retire bien le tag de la réponse suivante de `GET /api/shops/:id/videos`.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre à la boutique d'associer un produit de son catalogue à un intervalle de temps précis sur sa vidéo.\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js`\n- **Instructions d'exécution :** Ajouter `POST /api/videos/:id/tags` (body : `{ productId, startTime, endTime }`), vérifiant que la vidéo appartient à une boutique possédée par `req.user.uid` ET que le produit appartient à la même boutique (éviter le tag d'un produit d'une autre boutique). Ajouter `DELETE /api/videos/:id/tags/:tagId` (même vérification de propriété). Ajouter `GET /api/shops/:id/videos` (liste des vidéos de la boutique, tous statuts confondus, avec leurs tags — pour l'écran de gestion boutique).\n- **Critères de validation :** Taguer un produit de la boutique sur une vidéo qu'elle possède renvoie 201 ; taguer un produit d'une autre boutique est rejeté en 400 ; `DELETE` retire bien le tag de la réponse suivante de `GET /api/shops/:id/videos`.\n- **Statut :** [ ] Non commencé"}, {"id": 19, "title": "Ajouter la modération vidéo côté admin (backend)", "objective": "Permettre à un admin d'approuver ou rejeter une vidéo avant publication dans le feed (US-V1.5-01 : \"La vidéo n'est publiée dans le feed qu'après validation de la modération\"), en réutilisant le pattern d'approbation boutique déjà existant.", "files": "`server-sanhia/routes/admin.routes.js`", "instructions": "Ajouter `PATCH /api/admin/videos/:id/approve` (`status: \"approved\"`) et `PATCH /api/admin/videos/:id/reject` (`status: \"rejected\"`), suivant exactement le pattern de `PATCH /api/admin/shops/:id/approve` (Étape 12 de ROADMAP.md). Ajouter `GET /api/admin/videos` (liste paginée, filtrable par `status`, priorité implicite aux boutiques déjà `active` en triant sur le statut boutique si simple à faire, sinon tri par date de soumission — cf. recommandation du cahier §11 \"en priorisant les boutiques déjà validées\").", "validation": "`PATCH /api/admin/videos/:id/approve` sur une vidéo `pending` renvoie 200 avec `status: \"approved\"` ; un compte non-admin reçoit 403.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre à un admin d'approuver ou rejeter une vidéo avant publication dans le feed (US-V1.5-01 : \"La vidéo n'est publiée dans le feed qu'après validation de la modération\"), en réutilisant le pattern d'approbation boutique déjà existant.\n- **Fichiers impactés :** `server-sanhia/routes/admin.routes.js`\n- **Instructions d'exécution :** Ajouter `PATCH /api/admin/videos/:id/approve` (`status: \"approved\"`) et `PATCH /api/admin/videos/:id/reject` (`status: \"rejected\"`), suivant exactement le pattern de `PATCH /api/admin/shops/:id/approve` (Étape 12 de ROADMAP.md). Ajouter `GET /api/admin/videos` (liste paginée, filtrable par `status`, priorité implicite aux boutiques déjà `active` en triant sur le statut boutique si simple à faire, sinon tri par date de soumission — cf. recommandation du cahier §11 \"en priorisant les boutiques déjà validées\").\n- **Critères de validation :** `PATCH /api/admin/videos/:id/approve` sur une vidéo `pending` renvoie 200 avec `status: \"approved\"` ; un compte non-admin reçoit 403.\n- **Statut :** [ ] Non commencé"}, {"id": 20, "title": "Ajouter la route du fil vidéo public (backend)", "objective": "Exposer les vidéos approuvées, avec leurs tags produit, pour alimenter le feed client.", "files": "`server-sanhia/routes/shops.routes.js` (ou nouveau `server-sanhia/routes/videos.routes.js` si le fichier existant devient trop chargé — à juger sur place)", "instructions": "Ajouter `GET /api/videos/feed`, publique ou authentifiée selon le pattern déjà utilisé pour `GET /api/products` (suivre le même choix), paginée (`page`/`limit`, même convention que l'Étape 50 de ROADMAP.md), filtrée sur `status: \"approved\"`, incluant `shop` (nom, id) et `tags` avec leur `product` (au minimum `id, name, price, images[0]`, suffisant pour l'overlay).", "validation": "`GET /api/videos/feed` ne renvoie que des vidéos `approved` (une vidéo `pending` créée pour le test n'apparaît pas tant qu'elle n'est pas approuvée via l'Étape 19) ; chaque entrée inclut ses tags avec les infos produit nécessaires à l'overlay.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Exposer les vidéos approuvées, avec leurs tags produit, pour alimenter le feed client.\n- **Fichiers impactés :** `server-sanhia/routes/shops.routes.js` (ou nouveau `server-sanhia/routes/videos.routes.js` si le fichier existant devient trop chargé — à juger sur place)\n- **Instructions d'exécution :** Ajouter `GET /api/videos/feed`, publique ou authentifiée selon le pattern déjà utilisé pour `GET /api/products` (suivre le même choix), paginée (`page`/`limit`, même convention que l'Étape 50 de ROADMAP.md), filtrée sur `status: \"approved\"`, incluant `shop` (nom, id) et `tags` avec leur `product` (au minimum `id, name, price, images[0]`, suffisant pour l'overlay).\n- **Critères de validation :** `GET /api/videos/feed` ne renvoie que des vidéos `approved` (une vidéo `pending` créée pour le test n'apparaît pas tant qu'elle n'est pas approuvée via l'Étape 19) ; chaque entrée inclut ses tags avec les infos produit nécessaires à l'overlay.\n- **Statut :** [ ] Non commencé"}, {"id": 21, "title": "Créer l'outil de tagging vidéo (web, dashboard vendeur)", "objective": "Permettre à une boutique d'importer une vidéo et d'y associer des produits par intervalle, via des curseurs visuels (pas de saisie manuelle de temps, exigence explicite du cahier §5.2).", "files": "`sanhia-web/src/pages/Seller.jsx`", "instructions": "Nouvel onglet ou section \"Feed vidéo\" : upload vidéo (`POST /api/shops/:id/videos`), puis une fois la vidéo importée, afficher un lecteur `<video>` avec une timeline sous forme de barre horizontale sur laquelle l'utilisateur positionne 2 curseurs (début/fin) par glisser-déposer (utiliser `input[type=range]` en double, ou une implémentation `mousedown`/`mousemove` simple sur une div — pas de librairie externe nécessaire pour un besoin aussi ciblé), avec sélection du produit à taguer dans un `<select>` du catalogue existant de la boutique. Soumettre via `POST /api/videos/:id/tags`. Lister les tags déjà créés sous la timeline, avec suppression individuelle (`DELETE /api/videos/:id/tags/:tagId`). Afficher le statut de modération de la vidéo (`pending`/`approved`/`rejected`).", "validation": "Importer une vidéo, y positionner 2 curseurs, sélectionner un produit et valider crée un tag visible dans la liste sous la timeline, sans saisie manuelle de nombre à aucune étape.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Permettre à une boutique d'importer une vidéo et d'y associer des produits par intervalle, via des curseurs visuels (pas de saisie manuelle de temps, exigence explicite du cahier §5.2).\n- **Fichiers impactés :** `sanhia-web/src/pages/Seller.jsx`\n- **Instructions d'exécution :** Nouvel onglet ou section \"Feed vidéo\" : upload vidéo (`POST /api/shops/:id/videos`), puis une fois la vidéo importée, afficher un lecteur `<video>` avec une timeline sous forme de barre horizontale sur laquelle l'utilisateur positionne 2 curseurs (début/fin) par glisser-déposer (utiliser `input[type=range]` en double, ou une implémentation `mousedown`/`mousemove` simple sur une div — pas de librairie externe nécessaire pour un besoin aussi ciblé), avec sélection du produit à taguer dans un `<select>` du catalogue existant de la boutique. Soumettre via `POST /api/videos/:id/tags`. Lister les tags déjà créés sous la timeline, avec suppression individuelle (`DELETE /api/videos/:id/tags/:tagId`). Afficher le statut de modération de la vidéo (`pending`/`approved`/`rejected`).\n- **Critères de validation :** Importer une vidéo, y positionner 2 curseurs, sélectionner un produit et valider crée un tag visible dans la liste sous la timeline, sans saisie manuelle de nombre à aucune étape.\n- **Statut :** [ ] Non commencé"}, {"id": 22, "title": "Créer le fil vidéo immersif (web)", "objective": "Donner un équivalent web du \"5ème onglet\" mobile — une page dédiée au défilement de vidéos avec overlay produit dynamique.", "files": "`sanhia-web/src/pages/VideoFeed.jsx` (nouveau), `sanhia-web/src/App.jsx` (nouvelle route), `sanhia-web/src/components/layout/Header.jsx` (lien de nav)", "instructions": "Nouvelle route `/feed`, avec un conteneur à défilement vertical `scroll-snap-type: y mandatory` (CSS pur, pas de librairie), une `<video>` par section, chargeant `GET /api/videos/feed` (pagination infinie simple au scroll si raisonnable, sinon une première page suffit pour cette étape). Sur chaque vidéo, écouter l'événement `timeupdate` du lecteur pour trouver le tag dont `startTime <= currentTime < endTime` (recherche linéaire dans le tableau des tags de cette vidéo, largement suffisant vu le volume attendu) et afficher un bouton produit superposé (image + nom + prix) qui navigue vers `/produit/:id` au clic. Aucun bouton si aucun tag ne correspond à l'instant courant. Ajouter un lien \"Feed\" dans la navigation principale (`Header.jsx`).", "validation": "Ouvrir `/feed` fait défiler verticalement les vidéos approuvées ; sur une vidéo avec un tag, le bouton produit apparaît exactement pendant l'intervalle tagué (avant et après, absent) et le clic ouvre la fiche produit existante.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Donner un équivalent web du \"5ème onglet\" mobile — une page dédiée au défilement de vidéos avec overlay produit dynamique.\n- **Fichiers impactés :** `sanhia-web/src/pages/VideoFeed.jsx` (nouveau), `sanhia-web/src/App.jsx` (nouvelle route), `sanhia-web/src/components/layout/Header.jsx` (lien de nav)\n- **Instructions d'exécution :** Nouvelle route `/feed`, avec un conteneur à défilement vertical `scroll-snap-type: y mandatory` (CSS pur, pas de librairie), une `<video>` par section, chargeant `GET /api/videos/feed` (pagination infinie simple au scroll si raisonnable, sinon une première page suffit pour cette étape). Sur chaque vidéo, écouter l'événement `timeupdate` du lecteur pour trouver le tag dont `startTime <= currentTime < endTime` (recherche linéaire dans le tableau des tags de cette vidéo, largement suffisant vu le volume attendu) et afficher un bouton produit superposé (image + nom + prix) qui navigue vers `/produit/:id` au clic. Aucun bouton si aucun tag ne correspond à l'instant courant. Ajouter un lien \"Feed\" dans la navigation principale (`Header.jsx`).\n- **Critères de validation :** Ouvrir `/feed` fait défiler verticalement les vidéos approuvées ; sur une vidéo avec un tag, le bouton produit apparaît exactement pendant l'intervalle tagué (avant et après, absent) et le clic ouvre la fiche produit existante.\n- **Statut :** [ ] Non commencé"}, {"id": 23, "title": "Créer l'outil de tagging vidéo (mobile, côté vendeur)", "objective": "Porter l'import vidéo + tagging sur mobile, avec la même exigence de simplicité (curseurs, pas de saisie de temps).", "files": "`sanhia-mobile/app/(seller)/video-tagging.js` (nouveau)", "instructions": "Sélection vidéo via `expo-image-picker` (déjà utilisé, supporte aussi la vidéo en configurant `mediaTypes`), upload vers `POST /api/shops/:id/videos`. Une fois importée, lecteur `useVideoPlayer`/`VideoView` (pattern déjà utilisé dans `app/product/[id].js`) avec 2 curseurs tactiles sur une barre de progression personnalisée (`PanResponder` ou `Pressable` avec calcul de position au toucher — pas de librairie de slider externe si évitable, sinon vérifier une lib déjà présente dans les dépendances avant d'en ajouter une). Sélection du produit dans le catalogue de la boutique, soumission vers `POST /api/videos/:id/tags`.", "validation": "Depuis l'app mobile vendeur, importer une vidéo et y créer un tag produit aboutit à un tag visible côté web (Étape 21) pour la même vidéo.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Porter l'import vidéo + tagging sur mobile, avec la même exigence de simplicité (curseurs, pas de saisie de temps).\n- **Fichiers impactés :** `sanhia-mobile/app/(seller)/video-tagging.js` (nouveau)\n- **Instructions d'exécution :** Sélection vidéo via `expo-image-picker` (déjà utilisé, supporte aussi la vidéo en configurant `mediaTypes`), upload vers `POST /api/shops/:id/videos`. Une fois importée, lecteur `useVideoPlayer`/`VideoView` (pattern déjà utilisé dans `app/product/[id].js`) avec 2 curseurs tactiles sur une barre de progression personnalisée (`PanResponder` ou `Pressable` avec calcul de position au toucher — pas de librairie de slider externe si évitable, sinon vérifier une lib déjà présente dans les dépendances avant d'en ajouter une). Sélection du produit dans le catalogue de la boutique, soumission vers `POST /api/videos/:id/tags`.\n- **Critères de validation :** Depuis l'app mobile vendeur, importer une vidéo et y créer un tag produit aboutit à un tag visible côté web (Étape 21) pour la même vidéo.\n- **Statut :** [ ] Non commencé"}, {"id": 24, "title": "Créer le nouvel onglet Feed vidéo (mobile)", "objective": "Ajouter le nouvel onglet de défilement vidéo plein écran à la navigation principale acheteur (6ème onglet dans l'état réel actuel — voir constat n°3 en tête de document).", "files": "`sanhia-mobile/app/(buyer)/feed.js` (nouveau), `sanhia-mobile/app/(buyer)/_layout.js`", "instructions": "Nouvel écran avec une `FlatList` verticale `pagingEnabled` (ou `snapToInterval` = hauteur d'écran), chaque item plein écran contenant une vidéo (`useVideoPlayer`/`VideoView`) chargée depuis `GET /api/videos/feed`. Ne lire automatiquement que la vidéo actuellement visible à l'écran (via `onViewableItemsChanged` de `FlatList`), mettre les autres en pause — comportement attendu d'un feed type Reels, et nécessaire pour ne pas jouer plusieurs vidéos en simultané. Écouter le statut de lecture pour afficher le bouton produit superposé pendant l'intervalle tagué correspondant (même logique de recherche de tag que l'Étape 22), navigant vers `/product/:id` au tap. Enregistrer l'écran comme nouvel onglet dans `_layout.js` (ajout aux objets `ICONS`/`LABELS` existants et à la liste des `Tabs.Screen`).", "validation": "Le nouvel onglet apparaît dans la barre de navigation mobile ; y défiler verticalement joue une vidéo à la fois (les autres en pause) ; le bouton produit apparaît/disparaît selon l'intervalle tagué et ouvre la fiche produit au tap.", "originalStatus": "[ ] Non commencé", "status": "todo", "completedAt": "", "assignee": "", "note": "", "raw": "- **Objectif :** Ajouter le nouvel onglet de défilement vidéo plein écran à la navigation principale acheteur (6ème onglet dans l'état réel actuel — voir constat n°3 en tête de document).\n- **Fichiers impactés :** `sanhia-mobile/app/(buyer)/feed.js` (nouveau), `sanhia-mobile/app/(buyer)/_layout.js`\n- **Instructions d'exécution :** Nouvel écran avec une `FlatList` verticale `pagingEnabled` (ou `snapToInterval` = hauteur d'écran), chaque item plein écran contenant une vidéo (`useVideoPlayer`/`VideoView`) chargée depuis `GET /api/videos/feed`. Ne lire automatiquement que la vidéo actuellement visible à l'écran (via `onViewableItemsChanged` de `FlatList`), mettre les autres en pause — comportement attendu d'un feed type Reels, et nécessaire pour ne pas jouer plusieurs vidéos en simultané. Écouter le statut de lecture pour afficher le bouton produit superposé pendant l'intervalle tagué correspondant (même logique de recherche de tag que l'Étape 22), navigant vers `/product/:id` au tap. Enregistrer l'écran comme nouvel onglet dans `_layout.js` (ajout aux objets `ICONS`/`LABELS` existants et à la liste des `Tabs.Screen`).\n- **Critères de validation :** Le nouvel onglet apparaît dans la barre de navigation mobile ; y défiler verticalement joue une vidéo à la fois (les autres en pause) ; le bouton produit apparaît/disparaît selon l'intervalle tagué et ouvre la fiche produit au tap.\n- **Statut :** [ ] Non commencé\n\n---"}]}];
window.OUT_OF_SCOPE="## Hors périmètre d'exécution (décisions produit/budget, pas techniques)\n\nCes points, explicitement soulevés comme des hypothèses ouvertes par le cahier des charges lui-même, ne sont pas traités par les étapes ci-dessus et nécessitent un arbitrage séparé avant tout travail technique supplémentaire :\n\n- **Compression/streaming adaptatif vidéo** (cahier §5.1, §11) : arbitrage entre un service de streaming tiers et un traitement interne (ex. transcodage `ffmpeg` avant stockage R2), selon le budget technique disponible — aucune des deux options n'est implémentée par cette roadmap (stockage passthrough uniquement, identique au comportement déjà en place pour `Product.videoUrl`).\n- **Bêta fermée du feed vidéo** (cahier §9, §12) : recommandation de tester avec un groupe restreint de boutiques volontaires avant généralisation — décision de rollout, pas une étape de code ; peut se traduire techniquement par une simple restriction manuelle côté admin (ne pas approuver de vidéos hors du groupe pilote) sans développement dédié.\n- **Correction manuelle du point sur la carte** pour la précision d'adresse en zone dense (cahier §11) : nécessiterait l'intégration d'une librairie de carte interactive (aucune actuellement dans les dépendances web ou mobile) — non inclus dans la Phase A, qui se limite à la saisie texte + coordonnées déjà supportée par le modèle `Address` existant.";
